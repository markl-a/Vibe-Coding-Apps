# Flutter 跨平台移動應用
🤖 **AI-Driven | AI-Native** 🚀

使用 Flutter 框架開發的高性能跨平台移動應用，全面整合 AI 智能功能，一次開發支援 iOS、Android、Web 和桌面平台。

## 🌟 AI 增強亮點

本項目的所有 Flutter 應用都已整合先進的 AI 智能功能：

### ✨ Expense Tracker (記帳應用) - AI 增強版
- 🎯 **AI 智能分類** - 自動識別交易類別，準確率持續提升
- 💡 **財務健康分析** - 實時分析收支，提供個性化財務建議
- 🔮 **支出預測** - 基於歷史數據預測未來支出趨勢
- 📊 **智能預算建議** - AI 基於 50/30/20 法則推薦最佳預算
- 🔍 **異常檢測** - 自動識別異常支出模式
- 📸 **OCR 發票掃描** - 拍照自動識別發票資訊
- 📤 **多格式導出** - CSV/JSON/HTML 智能報表

### 🍳 Recipe App (食譜應用) - AI 增強版
- 🎯 **個性化推薦** - 基於 9 維度算法推薦最適合的食譜
- 📊 **營養分析** - 自動計算營養成分，提供健康評分（0-100分）
- 🔄 **食材替代** - 智能建議替代食材，適應素食/無麩質等需求
- 🛒 **智能購物清單** - 多食譜自動合併，按類別分組
- ⏱️ **烹飪計時器** - 多步驟計時，到時提醒
- ⭐ **評分系統** - 多維度評分和評論功能

### 🏃 Fitness Tracker (健身追蹤) - 計劃中
- AI 個性化訓練計劃
- AI 健康建議和目標設定
- 智能成就系統

📖 **詳細文檔**: [ENHANCEMENTS_SUMMARY.md](ENHANCEMENTS_SUMMARY.md)

---

## 📋 專案概述

Flutter 是 Google 開發的開源 UI 框架，使用 Dart 語言，以高性能和精美的原生界面著稱。Flutter 不僅支援移動平台，還可以編譯為 Web 和桌面應用，實現真正的全平台開發。

### 為什麼選擇 Flutter？

- **真正的跨平台**：一套程式碼支援 iOS、Android、Web、Windows、macOS、Linux
- **高性能**：直接編譯為原生機器碼，60/120fps 流暢運行
- **熱重載**：毫秒級重載，開發效率極高
- **精美 UI**：內建 Material Design 和 Cupertino 風格組件
- **Google 支援**：持續更新，生態系統快速發展
- **AI 友好**：Dart 語法清晰，AI 輔助開發效果優秀
- **Widget 體系**：一切皆為 Widget，組合式開發

## 🎯 適合開發的應用類型

### 電商應用
- 購物商城
- 產品展示
- 購物車與結帳
- 訂單追蹤

### 社交媒體
- 即時通訊
- 動態發布
- 圖片分享
- 短視頻應用

### 內容平台
- 新聞閱讀
- 影音串流
- 播客應用
- 電子書閱讀器

### 健康健身
- 運動追蹤
- 健身計劃
- 營養記錄
- 步數統計

### 生產力工具
- 待辦事項
- 筆記應用
- 日曆行事曆
- 時間管理

### 金融應用
- 記帳軟體
- 投資追蹤
- 預算管理
- 加密貨幣錢包

### 教育學習
- 線上課程
- 語言學習
- 題庫練習
- 兒童教育

### 遊戲
- 2D 休閒遊戲
- 益智遊戲
- 卡牌遊戲
- 輕量級 3D 遊戲（使用 Flame 引擎）

## 🛠️ 技術棧

### 核心框架
- **Flutter SDK** - UI 框架
- **Dart** - 程式語言（2.0+ / 3.0+）
- **Material Design** - Google 設計語言
- **Cupertino** - iOS 風格組件

### 狀態管理
- **Provider** - 官方推薦，簡單易用
- **Riverpod** - Provider 進化版，類型安全
- **Bloc / Cubit** - 基於事件驅動的狀態管理
- **GetX** - 輕量級全功能解決方案
- **MobX** - 響應式狀態管理
- **Redux** - 經典狀態管理模式

### 導航
- **Navigator 2.0** - Flutter 內建導航
- **go_router** - 聲明式路由
- **auto_route** - 自動生成路由
- **GetX Navigation** - GetX 內建導航

### 資料持久化
- **shared_preferences** - 簡單鍵值對儲存
- **Hive** - 輕量級 NoSQL 資料庫
- **sqflite** - SQLite 資料庫
- **Isar** - 高性能資料庫
- **ObjectBox** - 超快速物件資料庫
- **Drift** (Moor) - 反應式 SQLite

### 網路請求
- **dio** - 強大的 HTTP 客戶端
- **http** - Dart 官方 HTTP 庫
- **Retrofit** - 類型安全的 API 客戶端
- **GraphQL Flutter** - GraphQL 客戶端

### UI 組件庫
- **Flutter Material** - 內建 Material Design
- **Flutter Cupertino** - 內建 iOS 風格
- **GetWidget** - 1000+ UI 組件
- **Flutter Neumorphic** - 新擬態設計
- **Velocity_X** - 快速 UI 開發

### 多媒體
- **video_player** - 視頻播放
- **audioplayers** - 音頻播放
- **just_audio** - 進階音頻播放
- **image_picker** - 圖片選擇
- **camera** - 相機功能
- **photo_view** - 圖片縮放查看

### 動畫
- **AnimatedContainer** - 內建動畫容器
- **Hero** - 頁面切換動畫
- **Lottie** - Lottie 動畫支援
- **Rive** - 互動式動畫
- **flutter_animate** - 聲明式動畫

### 地圖與定位
- **google_maps_flutter** - Google 地圖
- **geolocator** - 地理定位
- **geocoding** - 地址編碼/解碼
- **flutter_map** - 開源地圖（OpenStreetMap）

### Firebase 整合
- **firebase_core** - Firebase 核心
- **firebase_auth** - 用戶認證
- **cloud_firestore** - NoSQL 雲端資料庫
- **firebase_storage** - 雲端儲存
- **firebase_messaging** - 推送通知
- **firebase_analytics** - 分析工具

### 工具與輔助
- **flutter_launcher_icons** - 自動生成應用圖標
- **flutter_native_splash** - 啟動畫面
- **url_launcher** - 打開 URL/電話/郵件
- **share_plus** - 分享功能
- **path_provider** - 檔案路徑
- **permission_handler** - 權限管理
- **connectivity_plus** - 網路狀態檢測

## 🚀 快速開始

### 安裝 Flutter

#### macOS
```bash
# 下載 Flutter SDK
git clone https://github.com/flutter/flutter.git -b stable

# 添加到 PATH
export PATH="$PATH:`pwd`/flutter/bin"

# 驗證安裝
flutter doctor

# 安裝 iOS 工具（需要 Xcode）
sudo xcode-select --switch /Applications/Xcode.app/Contents/Developer
sudo xcodebuild -runFirstLaunch
```

#### Windows
```bash
# 下載並解壓 Flutter SDK
# 添加 flutter\bin 到系統 PATH

# 驗證安裝
flutter doctor

# 安裝 Android 工具
# 下載 Android Studio 並安裝 Flutter/Dart 插件
```

#### Linux
```bash
# 下載 Flutter SDK
git clone https://github.com/flutter/flutter.git -b stable

# 添加到 PATH
export PATH="$PATH:`pwd`/flutter/bin"

# 驗證安裝
flutter doctor
```

### 創建新專案

```bash
# 創建 Flutter 專案
flutter create my_app

# 進入專案目錄
cd my_app

# 運行應用
flutter run

# 指定設備
flutter run -d chrome        # 瀏覽器
flutter run -d macos         # macOS
flutter run -d ios           # iOS 模擬器
flutter run -d android       # Android 模擬器
```

### 專案結構

```
my_app/
├── lib/                      # Dart 程式碼
│   ├── main.dart             # 應用入口
│   ├── models/               # 資料模型
│   ├── screens/              # 畫面/頁面
│   ├── widgets/              # 可重用組件
│   ├── services/             # API 服務
│   ├── providers/            # 狀態管理
│   ├── utils/                # 工具函數
│   ├── constants/            # 常量
│   └── theme/                # 主題配置
├── assets/                   # 資源文件
│   ├── images/               # 圖片
│   ├── fonts/                # 字體
│   └── data/                 # JSON 等數據文件
├── test/                     # 測試文件
├── android/                  # Android 原生代碼
├── ios/                      # iOS 原生代碼
├── web/                      # Web 配置
├── pubspec.yaml              # 依賴配置
└── README.md
```

### pubspec.yaml 範例

```yaml
name: my_app
description: A new Flutter project
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  # 狀態管理
  provider: ^6.0.0

  # 網路請求
  dio: ^5.0.0

  # 本地儲存
  shared_preferences: ^2.0.0
  hive: ^2.2.3
  hive_flutter: ^1.1.0

  # UI 組件
  cupertino_icons: ^1.0.0
  google_fonts: ^6.0.0

  # 工具
  intl: ^0.18.0
  url_launcher: ^6.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
  build_runner: ^2.4.0
  hive_generator: ^2.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/images/
    - assets/data/

  fonts:
    - family: CustomFont
      fonts:
        - asset: assets/fonts/CustomFont-Regular.ttf
        - asset: assets/fonts/CustomFont-Bold.ttf
          weight: 700
```

## 💡 範例專案：天氣應用

### main.dart

```dart
import 'package:flutter/material.dart';
import 'package:provider/provider.dart';
import 'screens/home_screen.dart';
import 'providers/weather_provider.dart';

void main() {
  runApp(const MyApp());
}

class MyApp extends StatelessWidget {
  const MyApp({super.key});

  @override
  Widget build(BuildContext context) {
    return MultiProvider(
      providers: [
        ChangeNotifierProvider(create: (_) => WeatherProvider()),
      ],
      child: MaterialApp(
        title: '天氣應用',
        debugShowCheckedModeBanner: false,
        theme: ThemeData(
          primarySwatch: Colors.blue,
          useMaterial3: true,
        ),
        home: const HomeScreen(),
      ),
    );
  }
}
```

### weather_provider.dart

```dart
import 'package:flutter/foundation.dart';
import 'package:dio/dio.dart';

class WeatherProvider extends ChangeNotifier {
  final Dio _dio = Dio();

  String _city = '台北';
  String _temperature = '--';
  String _description = '--';
  bool _isLoading = false;
  String? _error;

  String get city => _city;
  String get temperature => _temperature;
  String get description => _description;
  bool get isLoading => _isLoading;
  String? get error => _error;

  Future<void> fetchWeather(String cityName) async {
    _isLoading = true;
    _error = null;
    notifyListeners();

    try {
      final response = await _dio.get(
        'https://api.openweathermap.org/data/2.5/weather',
        queryParameters: {
          'q': cityName,
          'appid': 'YOUR_API_KEY',
          'units': 'metric',
          'lang': 'zh_tw',
        },
      );

      _city = cityName;
      _temperature = '${response.data['main']['temp'].round()}°C';
      _description = response.data['weather'][0]['description'];
    } catch (e) {
      _error = '無法獲取天氣資訊';
    } finally {
      _isLoading = false;
      notifyListeners();
    }
  }
}
```

### home_screen.dart

```dart
import 'package:flutter/material.dart';
import 'package:provider/provider.dart';
import '../providers/weather_provider.dart';

class HomeScreen extends StatefulWidget {
  const HomeScreen({super.key});

  @override
  State<HomeScreen> createState() => _HomeScreenState();
}

class _HomeScreenState extends State<HomeScreen> {
  final TextEditingController _controller = TextEditingController();

  @override
  void initState() {
    super.initState();
    // 初始載入天氣
    Future.microtask(
      () => context.read<WeatherProvider>().fetchWeather('台北')
    );
  }

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('天氣查詢'),
        elevation: 0,
      ),
      body: Consumer<WeatherProvider>(
        builder: (context, provider, child) {
          return Padding(
            padding: const EdgeInsets.all(20.0),
            child: Column(
              mainAxisAlignment: MainAxisAlignment.center,
              children: [
                // 城市搜尋
                TextField(
                  controller: _controller,
                  decoration: InputDecoration(
                    labelText: '輸入城市名稱',
                    border: OutlineInputBorder(
                      borderRadius: BorderRadius.circular(12),
                    ),
                    suffixIcon: IconButton(
                      icon: const Icon(Icons.search),
                      onPressed: () {
                        if (_controller.text.isNotEmpty) {
                          provider.fetchWeather(_controller.text);
                        }
                      },
                    ),
                  ),
                  onSubmitted: (value) {
                    if (value.isNotEmpty) {
                      provider.fetchWeather(value);
                    }
                  },
                ),
                const SizedBox(height: 40),

                // 天氣資訊卡片
                if (provider.isLoading)
                  const CircularProgressIndicator()
                else if (provider.error != null)
                  Text(
                    provider.error!,
                    style: const TextStyle(color: Colors.red),
                  )
                else
                  Card(
                    elevation: 4,
                    shape: RoundedRectangleBorder(
                      borderRadius: BorderRadius.circular(20),
                    ),
                    child: Padding(
                      padding: const EdgeInsets.all(30.0),
                      child: Column(
                        children: [
                          Text(
                            provider.city,
                            style: Theme.of(context).textTheme.headlineMedium,
                          ),
                          const SizedBox(height: 20),
                          Text(
                            provider.temperature,
                            style: Theme.of(context).textTheme.displayLarge?.copyWith(
                              fontWeight: FontWeight.bold,
                              color: Colors.blue,
                            ),
                          ),
                          const SizedBox(height: 10),
                          Text(
                            provider.description,
                            style: Theme.of(context).textTheme.titleLarge,
                          ),
                        ],
                      ),
                    ),
                  ),
              ],
            ),
          );
        },
      ),
    );
  }

  @override
  void dispose() {
    _controller.dispose();
    super.dispose();
  }
}
```

## 🤖 AI 輔助開發工作流程

### 1. 專案規劃
向 AI 描述應用需求：
```
"我想開發一個食譜分享 App，用戶可以瀏覽食譜、收藏喜歡的食譜、
搜尋食材，並上傳自己的食譜。請幫我規劃功能模組和資料結構。"
```

### 2. 技術選型
讓 AI 推薦技術方案：
```
"基於食譜應用的需求，推薦我使用的狀態管理方案、資料庫和後端服務。"
```

### 3. Widget 開發
使用 AI 生成組件：
```
"創建一個食譜卡片 Widget，顯示食譜圖片、標題、烹飪時間和難度星級，
支援點擊進入詳情頁。"
```

### 4. 狀態管理
AI 協助設計 Provider：
```
"使用 Riverpod 創建食譜列表的狀態管理，包含獲取、搜尋、收藏功能。"
```

### 5. API 整合
讓 AI 處理網路請求：
```
"使用 Dio 創建一個 API 服務類，包含獲取食譜列表、食譜詳情、
上傳食譜等功能，並處理錯誤和載入狀態。"
```

### 6. UI/UX 優化
AI 協助美化界面：
```
"為我的食譜列表添加下拉刷新和無限滾動功能，並使用漂亮的動畫效果。"
```

### 7. 問題排查
向 AI 描述錯誤：
```
"我在 iOS 上收到 'PlatformException: Failed to pick image' 錯誤，
並且 info.plist 已經添加了相機權限，如何解決？"
```

## 📊 專案範例清單

### 初級專案（1-2 週）
- ⭐ 計算機
- ⭐ 待辦事項
- ⭐ 天氣查詢
- ⭐ BMI 計算器
- ⭐ 記事本

### 中級專案（2-4 週）
- ⭐⭐ 新聞閱讀器
- ⭐⭐ 購物清單（CRUD）
- ⭐⭐ 音樂播放器
- ⭐⭐ 圖片瀏覽器
- ⭐⭐ 習慣追蹤器

### 高級專案（4+ 週）
- ⭐⭐⭐ 即時聊天應用
- ⭐⭐⭐ 電商平台
- ⭐⭐⭐ 社交媒體 App
- ⭐⭐⭐ 影片串流
- ⭐⭐⭐ 地圖導航應用

## 🔧 開發工具

### IDE
- **Android Studio** + Flutter/Dart 插件（推薦）
- **Visual Studio Code** + Flutter 擴充
- **IntelliJ IDEA** + Flutter 插件

### 偵錯工具
- **Flutter DevTools** - 官方偵錯工具
- **Dart DevTools** - Dart 偵錯器
- **Flutter Inspector** - Widget 樹檢查器

### 效能監控
- **Firebase Performance Monitoring**
- **Sentry** - 錯誤追蹤
- **Codemagic Insights** - CI/CD 監控

### 測試工具
- **flutter_test** - 單元測試
- **integration_test** - 整合測試
- **mockito** - Mock 測試
- **golden_toolkit** - UI 測試

### 建置與部署
- **Fastlane** - iOS/Android 自動化
- **Codemagic** - Flutter CI/CD
- **Bitrise** - 移動 CI/CD
- **GitHub Actions** - 自動化工作流

## 📚 學習資源

### 官方文檔
- [Flutter 官方文檔](https://flutter.dev/docs)
- [Dart 語言導覽](https://dart.dev/guides)
- [Flutter Widget 目錄](https://flutter.dev/docs/development/ui/widgets)
- [Flutter Cookbook](https://flutter.dev/docs/cookbook)

### 推薦課程
- Flutter & Dart - The Complete Guide (Udemy)
- Flutter 官方 Codelabs
- Google Developers - Flutter Tutorials

### 社群資源
- [Flutter Community](https://github.com/fluttercommunity)
- [Awesome Flutter](https://github.com/Solido/awesome-flutter)
- [Flutter Dev](https://www.reddit.com/r/FlutterDev/)
- [Flutter Discord](https://discord.gg/flutter)

### YouTube 頻道
- Flutter (官方)
- The Net Ninja - Flutter Tutorials
- Reso Coder
- Robert Brunhage
- Vandad Nahavandipoor

### 中文資源
- Flutter 中文網
- Flutter 台灣社群
- 掘金 - Flutter 專區

## ⚡ 效能優化建議

### 1. Widget 優化
```dart
// 使用 const 建構子
const Text('Hello');

// 避免不必要的重建
class MyWidget extends StatelessWidget {
  const MyWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text('Static content');
  }
}

// 使用 ListView.builder 而非 ListView
ListView.builder(
  itemCount: items.length,
  itemBuilder: (context, index) => ItemWidget(items[index]),
)
```

### 2. 圖片優化
```dart
// 使用 cached_network_image
CachedNetworkImage(
  imageUrl: imageUrl,
  placeholder: (context, url) => CircularProgressIndicator(),
  errorWidget: (context, url, error) => Icon(Icons.error),
)

// 指定圖片尺寸
Image.network(
  imageUrl,
  width: 100,
  height: 100,
  cacheWidth: 100,
  cacheHeight: 100,
)
```

### 3. 狀態管理優化
```dart
// 使用 Selector 避免不必要的重建
Selector<MyProvider, String>(
  selector: (context, provider) => provider.specificValue,
  builder: (context, value, child) {
    return Text(value);
  },
)
```

### 4. 打包優化
```bash
# 分析應用大小
flutter build apk --analyze-size

# 啟用混淆和優化
flutter build apk --obfuscate --split-debug-info=/<directory>

# 分 ABI 打包（減小體積）
flutter build apk --split-per-abi
```

## 🐛 常見問題與解決方案

### Gradle 建置問題
```bash
# 清理建置
flutter clean
flutter pub get
cd android && ./gradlew clean
```

### iOS Pod 問題
```bash
cd ios
rm -rf Pods Podfile.lock
pod install --repo-update
```

### 依賴衝突
```bash
flutter pub cache repair
flutter pub get
```

### 熱重載失效
```bash
# 完全重啟
flutter run --no-hot
```

## 🚀 發布應用

### Android (Google Play)

#### 1. 生成 Keystore
```bash
keytool -genkey -v -keystore ~/upload-keystore.jks \
  -keyalg RSA -keysize 2048 -validity 10000 \
  -alias upload
```

#### 2. 配置 key.properties
```properties
# android/key.properties
storePassword=<密碼>
keyPassword=<密碼>
keyAlias=upload
storeFile=<keystore 路徑>
```

#### 3. 建置 AAB
```bash
flutter build appbundle --release
```

### iOS (App Store)

#### 1. 在 Xcode 中配置
- 打開 `ios/Runner.xcworkspace`
- 配置 Bundle Identifier
- 設定簽名（Signing & Capabilities）

#### 2. 建置 Archive
```bash
flutter build ipa --release
```

#### 3. 上傳到 App Store Connect
使用 Xcode 的 Organizer 或 Transporter 上傳

### Web 部署
```bash
# 建置 Web
flutter build web --release

# 部署到 Firebase Hosting
firebase deploy --only hosting
```

## 🎯 最佳實踐

### 程式碼風格
```dart
// 使用 Dart 官方風格指南
// 安裝 flutter_lints
dev_dependencies:
  flutter_lints: ^2.0.0

// 啟用嚴格模式
analysis_options.yaml:
  include: package:flutter_lints/flutter.yaml
```

### Widget 拆分
```dart
// 將複雜 Widget 拆分為小組件
class ComplexScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: _buildAppBar(),
      body: Column(
        children: [
          _Header(),
          _Content(),
          _Footer(),
        ],
      ),
    );
  }
}
```

### 錯誤處理
```dart
// 全局錯誤處理
void main() {
  FlutterError.onError = (details) {
    // 記錄錯誤到 Crashlytics
    FirebaseCrashlytics.instance.recordFlutterError(details);
  };

  runApp(MyApp());
}
```

### 無障礙設計
```dart
// 添加語義標籤
Semantics(
  label: '提交按鈕',
  button: true,
  child: ElevatedButton(
    onPressed: () {},
    child: Text('提交'),
  ),
)
```

## 💡 進階主題

### 自定義繪製
```dart
class MyPainter extends CustomPainter {
  @override
  void paint(Canvas canvas, Size size) {
    final paint = Paint()
      ..color = Colors.blue
      ..style = PaintingStyle.fill;

    canvas.drawCircle(
      Offset(size.width / 2, size.height / 2),
      50,
      paint,
    );
  }

  @override
  bool shouldRepaint(CustomPainter oldDelegate) => false;
}
```

### 平台通道（Platform Channels）
```dart
// 調用原生代碼
const platform = MethodChannel('com.example.app/channel');

Future<void> callNativeMethod() async {
  try {
    final result = await platform.invokeMethod('getNativeData');
    print(result);
  } on PlatformException catch (e) {
    print('Failed: ${e.message}');
  }
}
```

### Isolates 多線程
```dart
import 'dart:isolate';

Future<void> runHeavyTask() async {
  final receivePort = ReceivePort();
  await Isolate.spawn(_isolateTask, receivePort.sendPort);

  final result = await receivePort.first;
  print(result);
}

void _isolateTask(SendPort sendPort) {
  // 執行耗時操作
  final result = heavyComputation();
  sendPort.send(result);
}
```

## 🤝 貢獻與協作

歡迎提交你的 Flutter 專案！

### 專案要求
- 使用 Flutter 3.0+
- 程式碼符合 Dart 風格指南
- 包含完整的 README
- 可正常建置和運行

### 提交流程
1. Fork 本倉庫
2. 創建功能分支
3. 編寫程式碼和測試
4. 提交 Pull Request

## 📄 授權

各專案請自行指定授權條款（MIT、Apache 2.0 等）。

---

**🚀 使用 Flutter 和 AI 打造精美的跨平台應用！**

**最後更新**: 2025-11-16
**維護狀態**: ✅ 活躍開發
