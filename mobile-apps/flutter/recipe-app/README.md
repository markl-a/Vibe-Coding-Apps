# 食譜分享應用 (Recipe App)

一個功能完整的 Flutter 食譜應用，提供豐富的食譜內容、收藏功能和烹飪指南。

## 功能特點

### 核心功能
- 🍳 **食譜瀏覽** - 瀏覽各種美食食譜
- 🔍 **搜尋功能** - 按名稱、食材或分類搜尋食譜
- ❤️ **收藏管理** - 收藏喜愛的食譜
- 📋 **詳細步驟** - 清楚的烹飪步驟說明
- ⏱️ **烹飪計時** - 內建計時器輔助烹飪
- 🏷️ **分類瀏覽** - 依料理類型、難度、時間篩選
- 📊 **營養資訊** - 卡路里和營養成分標示
- 📤 **分享功能** - 分享喜歡的食譜

### 技術特點
- 使用 Provider 進行狀態管理
- Hive 本地資料庫儲存收藏
- Dio 網路請求（可串接食譜 API）
- 精美的卡片式 UI 設計
- Material Design 3 設計風格
- 支援深色模式
- 流暢的圖片載入和緩存

## 安裝與運行

### 前置要求
- Flutter SDK 3.0+
- Dart 3.0+
- Android Studio / VS Code

### 安裝步驟

1. **進入專案目錄**
```bash
cd recipe-app
```

2. **安裝依賴**
```bash
flutter pub get
```

3. **生成 Hive 適配器**
```bash
flutter packages pub run build_runner build
```

4. **運行應用**
```bash
# Android
flutter run

# iOS
flutter run -d ios

# Web
flutter run -d chrome
```

## 專案結構

```
recipe-app/
├── lib/
│   ├── main.dart                 # 應用入口
│   ├── models/                   # 資料模型
│   │   ├── recipe.dart           # 食譜模型
│   │   ├── ingredient.dart       # 食材模型
│   │   └── category.dart         # 分類模型
│   ├── screens/                  # 頁面
│   │   ├── home_screen.dart      # 首頁
│   │   ├── recipe_detail_screen.dart
│   │   ├── search_screen.dart
│   │   ├── favorites_screen.dart
│   │   └── category_screen.dart
│   ├── widgets/                  # 可重用組件
│   │   ├── recipe_card.dart
│   │   ├── ingredient_item.dart
│   │   ├── step_item.dart
│   │   └── category_chip.dart
│   ├── providers/                # 狀態管理
│   │   └── recipe_provider.dart
│   ├── services/                 # 服務層
│   │   └── recipe_service.dart
│   └── utils/                    # 工具函數
│       ├── constants.dart
│       └── helpers.dart
├── assets/                       # 資源文件
│   ├── images/                   # 圖片資源
│   └── data/                     # 本地 JSON 數據
├── pubspec.yaml                  # 依賴配置
└── README.md
```

## 使用說明

### 瀏覽食譜
1. 在首頁瀏覽精選食譜
2. 點擊食譜卡片查看詳情
3. 滑動查看食材和步驟

### 搜尋食譜
1. 點擊搜尋圖標
2. 輸入關鍵字（食譜名稱或食材）
3. 從結果中選擇食譜

### 收藏食譜
1. 在食譜詳情頁點擊愛心圖標
2. 進入「收藏」頁面查看所有收藏
3. 長按可取消收藏

### 使用計時器
1. 在烹飪步驟中找到計時器圖標
2. 點擊啟動倒數計時
3. 時間到會有通知提醒

## 食譜分類

### 料理類型
- 🍜 中式料理
- 🍕 西式料理
- 🍱 日式料理
- 🌮 異國料理
- 🍰 甜點烘焙
- 🥗 健康輕食
- 🥤 飲品調酒
- 🍲 湯品燉煮

### 難度等級
- ⭐ 簡單 (30分鐘內)
- ⭐⭐ 中等 (30-60分鐘)
- ⭐⭐⭐ 困難 (60分鐘以上)

### 特殊標籤
- 🌱 素食
- 🥜 無麩質
- 🌶️ 辣味
- ❄️ 冷盤
- 🔥 熱炒

## 數據來源

應用支援兩種資料模式：

### 1. 本地模式（預設）
使用 `assets/data/recipes.json` 本地食譜資料

### 2. API 模式
可串接以下免費食譜 API：
- [TheMealDB](https://www.themealdb.com/api.php)
- [Spoonacular API](https://spoonacular.com/food-api)
- [Edamam Recipe API](https://developer.edamam.com/edamam-recipe-api)

在 `lib/services/recipe_service.dart` 中配置 API：

```dart
class RecipeService {
  final String apiKey = 'YOUR_API_KEY';
  final String baseUrl = 'https://api.example.com';

  Future<List<Recipe>> fetchRecipes() async {
    // API 請求邏輯
  }
}
```

## 自訂功能

### 添加新食譜

```dart
final newRecipe = Recipe(
  id: '1',
  title: '番茄炒蛋',
  description: '經典家常菜',
  imageUrl: 'https://example.com/image.jpg',
  prepTime: 10,
  cookTime: 15,
  servings: 2,
  difficulty: 'easy',
  ingredients: [
    Ingredient(name: '雞蛋', amount: '3顆'),
    Ingredient(name: '番茄', amount: '2顆'),
  ],
  steps: [
    '雞蛋打散',
    '番茄切塊',
    '熱鍋炒蛋',
  ],
);

provider.addRecipe(newRecipe);
```

### 修改主題色

在 `lib/main.dart` 中修改：

```dart
ColorScheme.fromSeed(
  seedColor: Colors.orange, // 修改為你喜歡的顏色
)
```

## 效能優化

- 使用 `CachedNetworkImage` 快取圖片
- ListView.builder 實現列表虛擬滾動
- Hero 動畫優化頁面切換
- 圖片預載入和壓縮

## 未來規劃

- [ ] 用戶登入 - 雲端同步收藏
- [ ] 食譜上傳 - 用戶自製食譜分享
- [ ] 社交功能 - 評論和評分系統
- [ ] AI 推薦 - 根據喜好推薦食譜
- [ ] 購物清單 - 自動生成採買清單
- [ ] AR 功能 - AR 輔助烹飪
- [ ] 語音助手 - 語音朗讀烹飪步驟
- [ ] 多語言支援

## 貢獻

歡迎提交 Issue 和 Pull Request！

## 授權

MIT License

## 致謝

- 食譜圖片來源: [Unsplash](https://unsplash.com/)
- 圖標: [Material Icons](https://fonts.google.com/icons)

---

**開發環境**: Flutter 3.16+ | Dart 3.0+
**最後更新**: 2025-11-16
**維護狀態**: ✅ 活躍開發
