# Dashboard Mobile App 📊

一個使用 **AI 輔助開發**的 React Native 儀表板應用。

## 功能特點

- 📊 儀表板 - 關鍵指標概覽
- 📈 數據分析 - 圖表和趨勢分析
- 📄 報表中心 - 各類報表生成
- ⚙️ 設定管理 - 應用設定和偏好

## 技術棧

- **框架**: React Native + Expo
- **導航**: React Navigation (Bottom Tabs)
- **狀態管理**: Zustand (可選)
- **語言**: TypeScript
- **圖標**: @expo/vector-icons (Ionicons)
- **圖表**: React Native SVG (自定義圖表)
- **日期處理**: date-fns

## 專案結構

```
dashboard-mobile-app/
├── src/
│   ├── screens/              # 頁面組件
│   │   ├── DashboardScreen.tsx
│   │   ├── AnalyticsScreen.tsx
│   │   ├── ReportsScreen.tsx
│   │   └── SettingsScreen.tsx
│   ├── components/           # 可重用組件
│   │   └── MetricCard.tsx
│   ├── navigation/           # 導航配置
│   ├── store/                # 狀態管理
│   ├── services/             # API 服務
│   ├── utils/                # 工具函數
│   └── types/                # TypeScript 類型定義
│       └── index.ts
├── App.tsx                   # 應用入口
├── app.json                  # Expo 配置
├── package.json
├── tsconfig.json
└── babel.config.js
```

## 開始使用

### 安裝依賴

```bash
npm install
# 或
yarn install
```

### 運行應用

```bash
# 啟動開發服務器
npm start

# 在 iOS 上運行
npm run ios

# 在 Android 上運行
npm run android

# 在網頁上運行
npm run web
```

## 主要功能

### 1. 儀表板 (Dashboard)

- 關鍵指標卡片
  - 總營收
  - 訂單數
  - 活躍用戶
  - 轉換率
- 最近活動列表
- 快速操作按鈕
- 即時數據更新

### 2. 數據分析 (Analytics)

- 銷售趨勢圖表（長條圖）
- 統計數據卡片
  - 平均訂單金額
  - 客戶滿意度
  - 回購率
  - 客單價成長
- 分類占比分析（進度條）
- 多維度數據展示

### 3. 報表中心 (Reports)

- 多種報表類型
  - 銷售報表
  - 客戶報表
  - 產品報表
  - 財務報表
  - 行銷報表
  - 自訂報表
- 最近查看記錄
- 報表匯出功能

### 4. 設定 (Settings)

- 一般設定
  - 推送通知
  - 深色模式
  - 自動重新整理
- 顯示設定
  - 語言選擇
  - 時區設定
  - 日期格式
- 帳號管理
  - 個人資料
  - 更改密碼
  - 隱私權設定
- 其他功能
  - 說明中心
  - 關於我們

## 組件說明

### MetricCard

可重用的指標卡片組件：

```typescript
<MetricCard
  data={{
    label: '總營收',
    value: 1245000,
    change: 12.5,
    trend: 'up',
    icon: 'cash-outline',
  }}
/>
```

## 數據模型

```typescript
interface MetricData {
  label: string;
  value: number;
  change: number;
  trend: 'up' | 'down';
  icon?: string;
}

interface SalesData {
  date: string;
  amount: number;
}

interface UserActivity {
  id: string;
  user: string;
  action: string;
  timestamp: Date;
  type: 'purchase' | 'signup' | 'login' | 'view';
}
```

## 圖表實現

使用原生 React Native 組件實現簡單的長條圖：

```typescript
const barHeight = (data.amount / maxValue) * 200;
```

對於更複雜的圖表需求，建議使用：
- `react-native-chart-kit`
- `victory-native`
- `react-native-svg-charts`

## 未來改進

- [ ] 整合真實的後端 API
- [ ] 實現更多圖表類型
- [ ] 添加數據篩選和日期範圍選擇
- [ ] 報表 PDF 生成和分享
- [ ] 推送通知整合
- [ ] 離線數據緩存
- [ ] 深色模式完整支持
- [ ] 即時數據更新（WebSocket）
- [ ] 自訂儀表板佈局
- [ ] 數據匯出（CSV, Excel）

## 設計特點

- **清晰的數據視覺化** - 使用圖表和指標卡片
- **響應式佈局** - 適配不同螢幕尺寸
- **直覺的導航** - Bottom Tabs 快速切換
- **一致的配色** - 使用 #5B5FFF 作為主色調
- **類型安全** - 完整的 TypeScript 支持

## AI 開發經驗

這個專案使用 AI 工具開發，展示了：

- 儀表板 UI/UX 設計模式
- 數據視覺化實現
- 自定義圖表組件
- 模組化組件設計
- TypeScript 最佳實踐
- React Native 性能優化

## 授權

MIT License
