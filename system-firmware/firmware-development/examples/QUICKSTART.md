# 🚀 快速開始指南

## 5 分鐘開始使用韌體範例

### 第一步：編譯所有範例

```bash
cd /home/user/Vibe-Coding-Apps/system-firmware/firmware-development/examples
make all
```

預期輸出：
```
創建輸出目錄...
編譯 basic/basic_firmware.c...
編譯 ota/ota_http_update.c...
編譯 crypto/aes_encrypt_decrypt.c...
編譯 crypto/rsa_signature_verify.c...
編譯 flash/flash_read_write.c...
編譯 bootloader/secure_boot_example.c...
編譯 complete-system/complete_firmware_system.c...
```

### 第二步：運行第一個範例

```bash
make run-basic
```

你將看到韌體啟動過程，包括：
- ✅ 硬體初始化
- ✅ 任務調度器啟動
- ✅ GPIO LED 閃爍
- ✅ 系統狀態報告
- ✅ 看門狗餵食

### 第三步：探索其他範例

#### OTA 更新範例
```bash
make run-ota
```
學習如何實現韌體遠程更新。

#### 加密範例
```bash
make run-crypto    # AES 加密解密
make run-rsa       # RSA 簽名驗證
```
學習如何保護你的韌體和數據。

#### Flash 操作範例
```bash
make run-flash
```
學習如何讀寫 Flash 存儲器。

#### 安全啟動範例
```bash
make run-boot
```
學習如何實現安全啟動流程。

#### 完整系統範例
```bash
make run-complete
```
查看一個完整的韌體系統如何整合所有功能。

### 第四步：運行所有範例

```bash
make run
```

這將依次運行所有範例，讓你看到完整的演示。

## 📁 範例文件說明

| 文件 | 功能 | 學習重點 |
|------|------|----------|
| `basic/basic_firmware.c` | 基本韌體框架 | 初始化流程、任務調度 |
| `ota/ota_http_update.c` | HTTP OTA 更新 | 遠程更新、分區管理 |
| `crypto/aes_encrypt_decrypt.c` | AES 加密 | 對稱加密、填充機制 |
| `crypto/rsa_signature_verify.c` | RSA 簽名 | 數字簽名、完整性驗證 |
| `flash/flash_read_write.c` | Flash 操作 | 存儲器管理、CRC 校驗 |
| `bootloader/secure_boot_example.c` | 安全啟動 | 簽名驗證、信任鏈 |
| `complete-system/complete_firmware_system.c` | 完整系統 | 系統集成、模組化設計 |

## 🔧 自定義範例

### 修改韌體版本

編輯任意 `.c` 文件，找到版本定義：

```c
#define FW_VERSION_MAJOR  1
#define FW_VERSION_MINOR  0
#define FW_VERSION_PATCH  0
```

修改後重新編譯：
```bash
make clean
make all
```

### 添加自己的任務

在 `basic_firmware.c` 中添加新任務：

```c
void task_my_custom(void) {
    printf("[CUSTOM] 我的自定義任務\n");
    // 你的代碼
}

// 添加到任務列表
task_t task_list[] = {
    // ... 現有任務 ...
    { task_my_custom, 2000, 0, true, "My Custom Task" },
};
```

## 🐛 故障排除

### 編譯錯誤

**問題**: `gcc: command not found`

**解決**: 安裝 GCC 編譯器
```bash
sudo apt-get update
sudo apt-get install build-essential
```

### 運行錯誤

**問題**: `Permission denied`

**解決**: 添加執行權限
```bash
chmod +x build/*
```

### 清理重建

如果遇到奇怪的問題，試試清理重建：
```bash
make clean
make all
```

## 💡 下一步

1. **閱讀完整文檔**: 查看 `README.md` 了解詳細信息
2. **修改範例代碼**: 嘗試修改範例以理解工作原理
3. **移植到硬體**: 參考文檔將範例移植到實際的 MCU
4. **學習最佳實踐**: 研究範例中的編碼模式和設計決策

## 📚 推薦學習路徑

### 初學者
1. ✅ 運行 `basic_firmware` - 理解基本結構
2. ✅ 運行 `flash_read_write` - 學習存儲操作
3. ✅ 運行 `aes_encrypt_decrypt` - 了解加密基礎

### 進階
4. ✅ 運行 `ota_http_update` - 實現 OTA 功能
5. ✅ 運行 `secure_boot_example` - 加強安全性
6. ✅ 運行 `complete_firmware_system` - 整合所有功能

### 專家
7. 🔧 修改範例代碼以適應你的需求
8. 🔧 移植到實際硬體平台 (STM32, ESP32)
9. 🔧 添加新功能並貢獻回專案

## ❓ 獲得幫助

- 查看 `README.md` 中的常見問題
- 閱讀源代碼中的註釋
- 參考相關技術文檔

祝你學習愉快！ 🎉
