# AI 線索評分系統使用指南

## 概述

AI 線索評分系統使用智能算法自動評估線索質量，幫助銷售團隊優先處理高質量線索，提高轉化率。

## 核心功能

### 1. 自動評分引擎

系統基於以下維度自動計算線索評分（0-100分）：

#### 評分維度

| 維度 | 權重 | 說明 |
|------|------|------|
| 人口統計學評分 | 25% | 基於公司、行業、職位等基本信息 |
| 行為評分 | 30% | 基於線索的互動行為和活動記錄 |
| 參與度評分 | 25% | 基於響應程度和活躍度 |
| 預算評分 | 10% | 基於預算範圍 |
| 時機評分 | 10% | 基於時效性和新鮮度 |

#### 評級系統

根據總分自動分配評級：
- **熱門 (Hot)**: 75-100 分 - 高優先級，立即跟進
- **溫和 (Warm)**: 50-74 分 - 中等優先級，定期跟進
- **冷淡 (Cold)**: 0-49 分 - 低優先級，長期培育

### 2. AI 智能建議

系統會根據線索的評分和特徵自動生成行動建議，包括：
- 跟進優先級建議
- 聯繫方式建議
- 內容推薦建議
- 時機建議

## API 使用指南

### 1. 為單個線索計算 AI 評分

```bash
POST /api/leads/{id}/calculate_ai_score/
Authorization: Token your_token_here
```

**響應示例：**
```json
{
  "message": "AI 評分計算完成",
  "old_score": 45,
  "new_score": 78,
  "old_rating": "cold",
  "new_rating": "warm",
  "scores_detail": {
    "demographic_score": 75,
    "behavior_score": 82,
    "engagement_score": 70,
    "budget_score": 85,
    "timing_score": 80
  },
  "recommendation": "⭐ 中等質量線索，建議在2-3天內聯繫。\n- 提高參與度：分享相關案例研究或白皮書。"
}
```

### 2. 預測線索轉化概率

```bash
GET /api/leads/{id}/predict_conversion/
Authorization: Token your_token_here
```

**響應示例：**
```json
{
  "lead_id": 123,
  "lead_name": "張 三",
  "current_score": 78,
  "calculated_score": 78,
  "scores_detail": {
    "demographic_score": 75,
    "behavior_score": 82,
    "engagement_score": 70,
    "budget_score": 85,
    "timing_score": 80
  },
  "conversion_probability": 62.4,
  "recommendation": "⭐ 中等質量線索，建議在2-3天內聯繫。"
}
```

### 3. 批量計算評分

```bash
POST /api/leads/batch_score/
Authorization: Token your_token_here
Content-Type: application/json

{
  "lead_ids": [1, 2, 3, 4, 5]  // 可選，不提供則為所有線索
}
```

**響應示例：**
```json
{
  "message": "批量評分完成",
  "updated_count": 5,
  "total_leads": 5
}
```

### 4. 獲取評分分析報告

```bash
GET /api/leads/score_analytics/
Authorization: Token your_token_here
```

**響應示例：**
```json
{
  "score_distribution": {
    "excellent": 45,
    "good": 78,
    "average": 120,
    "poor": 35
  },
  "conversion_by_score": [
    {
      "range": "excellent",
      "min_score": 80,
      "max_score": 100,
      "total_leads": 45,
      "converted_leads": 32,
      "conversion_rate": 71.11
    },
    {
      "range": "good",
      "min_score": 60,
      "max_score": 80,
      "total_leads": 78,
      "converted_leads": 38,
      "conversion_rate": 48.72
    }
  ]
}
```

## 管理命令使用

### 1. 批量計算所有線索評分

```bash
python manage.py score_leads --all
```

### 2. 只為新線索計算評分

```bash
python manage.py score_leads --new
```

### 3. 重新評分舊線索

```bash
python manage.py score_leads --days 30  # 重新評分30天前的線索
```

### 4. 為特定狀態的線索計算評分

```bash
python manage.py score_leads --status contacted
```

### 5. 查看評分分析報告

```bash
python manage.py score_leads --analytics
```

**輸出示例：**
```
=== 線索評分分析報告 ===

📊 評分分佈：
  excellent :   45 ( 16.1%)
  good      :   78 ( 27.9%)
  average   :  120 ( 42.9%)
  poor      :   35 ( 12.5%)

📈 評分範圍轉化率：
  Excellent  (80-100):   45 線索,  32 轉化,  71.1% 轉化率
  Good       (60-79):    78 線索,  38 轉化,  48.7% 轉化率
  Average    (40-59):   120 線索,  35 轉化,  29.2% 轉化率
  Poor       (0-39):     35 線索,   3 轉化,   8.6% 轉化率
```

## Python 代碼使用示例

### 1. 為單個線索計算評分

```python
from leads.models import Lead
from leads.ai_scoring import LeadScoringEngine

# 獲取線索
lead = Lead.objects.get(id=1)

# 創建評分引擎
engine = LeadScoringEngine(lead)

# 計算評分
total_score, scores_detail = engine.calculate_score()
print(f"總分: {total_score}")
print(f"詳細評分: {scores_detail}")

# 獲取建議評級
rating = engine.suggest_rating(total_score)
print(f"建議評級: {rating}")

# 生成行動建議
recommendation = engine.generate_recommendation(total_score, scores_detail)
print(f"行動建議:\n{recommendation}")

# 更新線索
lead.score = total_score
lead.rating = rating
lead.save()
```

### 2. 批量評分

```python
from leads.ai_scoring import BatchLeadScorer

# 為所有線索評分
updated_count = BatchLeadScorer.score_all_leads()
print(f"更新了 {updated_count} 個線索")

# 只為新線索評分
updated_count = BatchLeadScorer.score_new_leads()

# 重新評分30天前的線索
updated_count = BatchLeadScorer.rescore_old_leads(days=30)
```

### 3. 線索分析

```python
from leads.ai_scoring import LeadAnalytics

# 獲取評分分佈
distribution = LeadAnalytics.get_score_distribution()
print(f"優秀線索: {distribution['excellent']}")
print(f"良好線索: {distribution['good']}")

# 獲取轉化率分析
conversion_data = LeadAnalytics.get_conversion_by_score()
for data in conversion_data:
    print(f"{data['range']}: {data['conversion_rate']}% 轉化率")

# 預測單個線索轉化概率
lead = Lead.objects.get(id=1)
probability = LeadAnalytics.predict_conversion_probability(lead)
print(f"轉化概率: {probability * 100:.1f}%")
```

## 評分算法詳解

### 1. 人口統計學評分

評估標準：
- **行業匹配度**: 科技(90)、金融(85)、製造(75)、零售(70)等
- **公司規模**: 大型企業(90)、中型企業(75)、小型企業(60)等
- **職位級別**: C級高管、VP、總監等高級職位加分
- **公司信息完整性**: 公司名稱、規模等信息完整性

### 2. 行為評分

評估標準：
- **活動記錄數量**: 每個活動記錄加5分（最多30分）
- **活動類型多樣性**: 每種不同類型加5分
- **最近活動**: 最近7天的活動加分

### 3. 參與度評分

評估標準：
- **來源質量**: 推薦(90)、活動(80)、網站(70)等
- **最後聯繫時間**: 越近評分越高
- **線索狀態**: 已確認(80)、已聯繫(60)、新線索(50)等
- **郵箱類型**: 企業郵箱加分

### 4. 預算評分

評估標準：
- ≥ 100萬: 100分
- 50-100萬: 85分
- 10-50萬: 70分
- 5-10萬: 55分
- < 5萬: 40分

### 5. 時機評分

評估標準：
- **線索新鮮度**: 創建時間越近評分越高
- **更新頻率**: 最近更新的線索加分

## 最佳實踐

### 1. 自動化評分

**建議設置定時任務**：

```python
# 在 Celery 中設置定期任務
from celery import shared_task
from leads.ai_scoring import BatchLeadScorer

@shared_task
def auto_score_leads():
    """每天自動評分新線索"""
    updated = BatchLeadScorer.score_new_leads()
    return f"Updated {updated} leads"

# 在 celery beat 中配置
CELERY_BEAT_SCHEDULE = {
    'score-new-leads': {
        'task': 'leads.tasks.auto_score_leads',
        'schedule': crontab(hour=2, minute=0),  # 每天凌晨2點執行
    },
}
```

### 2. 評分觸發器

**在創建或更新線索時自動計算評分**：

```python
# 在 models.py 中添加信號
from django.db.models.signals import post_save
from django.dispatch import receiver

@receiver(post_save, sender=Lead)
def auto_score_lead(sender, instance, created, **kwargs):
    """創建或更新線索時自動計算評分"""
    if created or instance.score == 0:
        from leads.ai_scoring import LeadScoringEngine
        engine = LeadScoringEngine(instance)
        score, _ = engine.calculate_score()
        rating = engine.suggest_rating(score)

        # 避免遞歸調用
        Lead.objects.filter(pk=instance.pk).update(
            score=score,
            rating=rating
        )
```

### 3. 監控和優化

**定期查看評分效果**：

```bash
# 每週查看評分分析報告
python manage.py score_leads --analytics

# 對比評分與實際轉化率
python manage.py shell
>>> from leads.ai_scoring import LeadAnalytics
>>> data = LeadAnalytics.get_conversion_by_score()
>>> # 分析高分線索的實際轉化率是否符合預期
```

### 4. 個性化調整

根據業務特點調整評分權重：

```python
# 修改 ai_scoring.py 中的權重配置
weights = {
    'demographic_score': 0.30,  # 增加人口統計學權重
    'behavior_score': 0.25,     # 減少行為權重
    'engagement_score': 0.25,
    'budget_score': 0.15,       # 增加預算權重
    'timing_score': 0.05,       # 減少時機權重
}
```

## 常見問題

### Q: 評分多久更新一次？

A: 有三種更新方式：
1. 手動通過 API 觸發更新
2. 使用管理命令批量更新
3. 設置自動化任務定期更新

### Q: 如何提高線索評分？

A: 可以通過以下方式提高評分：
1. 增加互動活動記錄
2. 完善線索基本信息（公司、行業、職位）
3. 使用企業郵箱
4. 定期跟進保持活躍度
5. 確認預算信息

### Q: 評分算法可以自定義嗎？

A: 可以。修改 `leads/ai_scoring.py` 文件中的評分邏輯和權重配置。

### Q: 如何解讀轉化概率？

A: 轉化概率基於歷史數據和當前評分計算：
- 80%以上: 極高轉化可能，優先跟進
- 60-80%: 較高轉化可能，重點關注
- 40-60%: 中等轉化可能，持續培育
- 40%以下: 較低轉化可能，長期培育

## 技術架構

```
┌─────────────────────────────────────────┐
│         LeadScoringEngine               │
│  ┌───────────────────────────────────┐  │
│  │  計算各維度評分                    │  │
│  │  - demographic_score               │  │
│  │  - behavior_score                  │  │
│  │  - engagement_score                │  │
│  │  - budget_score                    │  │
│  │  - timing_score                    │  │
│  └───────────────────────────────────┘  │
│  ┌───────────────────────────────────┐  │
│  │  加權計算總分                      │  │
│  └───────────────────────────────────┘  │
│  ┌───────────────────────────────────┐  │
│  │  生成評級和建議                    │  │
│  └───────────────────────────────────┘  │
└─────────────────────────────────────────┘
              ↓
┌─────────────────────────────────────────┐
│         BatchLeadScorer                 │
│  - 批量評分所有線索                     │
│  - 只評分新線索                         │
│  - 重新評分舊線索                       │
└─────────────────────────────────────────┘
              ↓
┌─────────────────────────────────────────┐
│         LeadAnalytics                   │
│  - 評分分佈統計                         │
│  - 轉化率分析                           │
│  - 轉化概率預測                         │
└─────────────────────────────────────────┘
```

## 進階功能（未來規劃）

### 1. 機器學習模型

使用歷史轉化數據訓練 ML 模型：
- 使用 scikit-learn 訓練分類模型
- 基於特徵工程優化預測準確度
- 定期重新訓練模型

### 2. A/B 測試

測試不同評分策略的效果：
- 對照組使用默認評分
- 實驗組使用調整後的評分
- 比較轉化率差異

### 3. 實時評分

線索行為發生時立即更新評分：
- 使用 WebSocket 推送評分變化
- 集成到 CRM 前端界面
- 實時通知銷售人員

---

**📞 需要幫助？** 請查看項目文檔或聯繫開發團隊。
