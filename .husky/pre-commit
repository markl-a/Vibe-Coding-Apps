#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

echo "🚀 Running pre-commit checks..."
echo ""

# Run lint-staged (handles TypeScript, ESLint, Prettier, console.log checks)
echo "📝 Running lint-staged..."
pnpm lint-staged

echo ""
echo "✅ Pre-commit checks completed successfully!"
