# Godot AI 輔助開發指南
🤖 **AI-Native Development** 🚀

本指南介紹如何使用 AI 輔助工具加速 Godot 遊戲開發。

## 📋 目錄

- [AI 工具概述](#ai-工具概述)
- [內建 AI 工具](#內建-ai-工具)
- [AI 提示詞模板](#ai-提示詞模板)
- [實戰案例](#實戰案例)
- [最佳實踐](#最佳實踐)
- [Godot 特有技巧](#godot-特有技巧)

---

## 🤖 AI 工具概述

### AI 工具的優勢

使用 AI 輔助 Godot 開發的好處:

- ⚡ **快速原型** - 幾分鐘生成 GDScript 代碼
- 📚 **學習資源** - 透過 AI 代碼學習 Godot 最佳實踐
- 🐛 **調試助手** - 快速找到並修復問題
- 💡 **創意激發** - AI 提供多種實現方案
- 🎨 **場景設置** - 自動生成場景結構指南

### 本專案的 AI 工具

位於 `scripts/AIHelper.gd` 的 AI 輔助工具提供:

1. **預設提示詞模板** - 常用功能快速生成
2. **代碼生成提示詞** - 自定義腳本創建
3. **調試提示詞** - 問題排查
4. **優化提示詞** - 性能改進
5. **場景設置提示詞** - 節點配置指南

---

## 🛠️ 內建 AI 工具

### AIHelper 類

#### 使用方法

**方法 1: 在場景中使用**

```gdscript
# 添加 AIHelper 節點到場景
# 或在任意腳本中調用

func _ready():
    # 生成玩家控制器提示詞
    var prompt = AIHelper.generate_common_prompt("player")
    AIHelper.print_prompt(prompt)
    AIHelper.copy_to_clipboard(prompt)
```

**方法 2: 作為 EditorScript 運行**

```gdscript
@tool
extends EditorScript

func _run():
    var prompt = AIHelper.generate_common_prompt("enemy")
    print(prompt)
    DisplayServer.clipboard_set(prompt)
```

運行: `File` > `Run` (需要先將腳本設為 @tool)

**方法 3: 在代碼中直接調用**

```gdscript
# 創建自定義腳本提示詞
var prompt = AIHelper.get_create_script_prompt(
    "WeaponSystem",
    "武器系統，支援多種武器切換",
    [
        "武器數據結構",
        "武器切換邏輯",
        "彈藥管理",
        "射擊動畫"
    ]
)

print(prompt)
DisplayServer.clipboard_set(prompt)
```

### 可用的模板

```gdscript
AIHelper.generate_common_prompt("player")      # 玩家控制器
AIHelper.generate_common_prompt("enemy")       # 敵人 AI
AIHelper.generate_common_prompt("inventory")   # 物品欄系統
AIHelper.generate_common_prompt("ui")          # UI 管理器
AIHelper.generate_common_prompt("save")        # 存檔系統
AIHelper.generate_common_prompt("state")       # 狀態機
AIHelper.generate_common_prompt("audio")       # 音效管理器
AIHelper.generate_common_prompt("pool")        # 對象池
AIHelper.generate_common_prompt("camera")      # 相機控制器
AIHelper.generate_common_prompt("dialog")      # 對話系統
```

---

## 📝 AI 提示詞模板

### 1. 創建新 GDScript

```
創建一個 Godot GDScript 'ScriptName'

描述: [功能描述]

功能需求:
- 功能 1
- 功能 2
- 功能 3

請提供:
1. 完整的 GDScript 代碼 (Godot 4.x)
2. 使用 @export 變數
3. 使用 @onready 獲取節點
4. 添加文檔註解 (##)
5. 錯誤處理

代碼風格:
- snake_case: 變數和函數
- PascalCase: 類名
- UPPER_CASE: 常量
```

### 2. 擴展現有腳本

```
在以下 Godot GDScript 中添加 [新功能]:

現有代碼:
```gdscript
[貼上代碼]
```

新功能需求:
- [具體需求]

請提供:
1. 修改後的完整代碼
2. 標註新增部分
3. 使用說明
4. 需要的節點設置
```

### 3. 調試 Godot 錯誤

```
Godot 遊戲出現以下錯誤:

錯誤訊息:
[錯誤訊息]

代碼片段:
```gdscript
[代碼]
```

場景結構:
[描述節點結構]

請分析問題並提供:
1. 錯誤原因
2. 修復代碼
3. Godot 特定注意事項
4. 預防措施
```

### 4. 場景設置

```
創建 Godot 場景設置指南:

場景名稱: [場景名]
用途: [場景用途]

節點結構:
[描述節點樹]

請提供:
1. 詳細的節點添加步驟
2. Inspector 設置
3. 腳本附加說明
4. 信號連接步驟
5. 常見問題
```

### 5. 性能優化

```
優化以下 Godot GDScript 性能:

```gdscript
[代碼]
```

目標:
- 減少 draw calls
- 優化物理計算
- 降低內存使用
- 使用對象池

請提供優化後的代碼和說明。
```

---

## 💡 實戰案例

### 案例 1: 創建簡單的跳躍功能

**AI 提示詞:**
```
在 Godot CharacterBody2D 腳本中添加跳躍功能:

使用 Godot 4.x 語法

需求:
1. 按空格跳躍
2. 使用重力
3. 地面檢測 (使用 is_on_floor())
4. 可變跳躍高度 (按住空格跳更高)
5. @export 變數: jump_velocity, gravity

請提供完整的 _physics_process 實現。
```

**AI 回覆示例:**
```gdscript
extends CharacterBody2D

@export var speed = 300.0
@export var jump_velocity = -400.0
@export var gravity = 980.0

func _physics_process(delta):
    # 應用重力
    if not is_on_floor():
        velocity.y += gravity * delta

    # 跳躍
    if Input.is_action_just_pressed("ui_accept") and is_on_floor():
        velocity.y = jump_velocity

    # 可變跳躍高度
    if Input.is_action_just_released("ui_accept") and velocity.y < 0:
        velocity.y *= 0.5

    # 移動
    var direction = Input.get_axis("ui_left", "ui_right")
    velocity.x = direction * speed

    move_and_slide()
```

### 案例 2: 創建道具生成系統

**AI 提示詞:**
```
創建一個 Godot 道具生成系統:

需求:
1. 定時在隨機位置生成道具
2. 道具類型: 生命、金幣、加速
3. 使用 Timer 節點
4. 生成在螢幕範圍內
5. 道具場景通過 @export 設置
6. 對象池優化 (可選)

請提供:
- PowerUpSpawner.gd 腳本
- 使用說明
- 場景結構建議
```

**AI 回覆會包含:**
- 完整的生成器腳本
- Timer 配置說明
- 場景結構建議
- 隨機位置計算邏輯

### 案例 3: 實現簡單的對話系統

**AI 提示詞:**
```
創建一個 Godot 對話系統:

需求:
1. 對話數據使用 Resource
2. 打字機效果 (逐字顯示)
3. 對話框 UI (Label + Panel)
4. 點擊繼續下一句
5. 對話結束信號
6. 支援角色名稱顯示

請提供:
- DialogSystem.gd
- DialogData.gd (Resource)
- 使用示例
- UI 節點結構
```

---

## 🎯 最佳實踐

### 1. 編寫清晰的提示詞

**✅ 好的提示詞:**
```
創建一個 Godot GDScript 敵人巡邏 AI:
- 繼承自 CharacterBody2D
- 在兩個 Marker2D 節點間移動
- 使用 move_and_slide()
- 碰到牆壁時轉向
- 巡邏速度可在 Inspector 調整
- 使用 @export 和 @onready
- Godot 4.x 語法
```

**❌ 不好的提示詞:**
```
做一個敵人AI
```

### 2. 提供 Godot 特定上下文

總是包含:
- Godot 版本 (4.x 或 3.x)
- 節點類型 (CharacterBody2D, Area2D 等)
- 場景結構
- 使用的內建功能

### 3. 明確 Godot 4.x vs 3.x

**Godot 4.x 特有:**
```
請使用 Godot 4.x 語法:
- @export 而非 export
- @onready 而非 onready
- move_and_slide() 無參數
- Vector2i 替代 Vector2 (用於 tilemap)
```

**Godot 3.x 特有:**
```
請使用 Godot 3.x 語法:
- export var
- move_and_slide(velocity)
- KinematicBody2D 而非 CharacterBody2D
```

### 4. 請求場景設置說明

```
請同時提供:
1. 完整的 GDScript 代碼
2. 所需的節點結構
3. Inspector 設置
4. 輸入映射 (如需要)
5. 信號連接步驟
```

### 5. 迭代改進

```
第一次: 創建基礎功能
"創建簡單的敵人巡邏 AI"

第二次: 添加功能
"在上述代碼基礎上添加檢測玩家功能"

第三次: 優化
"優化性能，使用對象池管理敵人"
```

---

## 🎮 Godot 特有技巧

### 1. 節點引用

**提示詞:**
```
在 GDScript 中正確獲取節點引用:
- 使用 @onready
- 使用 $ 簡寫或 get_node()
- 空值檢查
- 跨場景引用

請提供最佳實踐示例。
```

**示例:**
```gdscript
@onready var sprite = $Sprite2D
@onready var collision = $CollisionShape2D
@onready var timer = get_node("Timer")

func _ready():
    if sprite:
        sprite.visible = true
```

### 2. 信號系統

**提示詞:**
```
創建自定義信號系統:
- 定義信號
- 發射信號
- 連接信號 (代碼和編輯器)
- 信號參數傳遞
- 最佳實踐

使用 Godot 4.x 語法。
```

**示例:**
```gdscript
# 定義信號
signal health_changed(new_health, max_health)
signal died

# 發射信號
func take_damage(amount):
    health -= amount
    health_changed.emit(health, max_health)

    if health <= 0:
        died.emit()

# 連接信號
func _ready():
    died.connect(_on_died)
    # 或使用 lambda
    health_changed.connect(func(h, m): print("Health: %d/%d" % [h, m]))
```

### 3. 場景實例化

**提示詞:**
```
在 Godot 中動態實例化場景:
- 使用 PackedScene
- @export 場景變數
- instantiate() 方法
- add_child() 正確使用
- 清理和 queue_free()

請提供完整示例。
```

**示例:**
```gdscript
@export var enemy_scene: PackedScene

func spawn_enemy():
    if enemy_scene:
        var enemy = enemy_scene.instantiate()
        enemy.global_position = spawn_point.global_position
        add_child(enemy)
```

### 4. 資源 (Resource) 使用

**提示詞:**
```
創建自定義 Godot Resource:
- 繼承 Resource
- @export 變數
- 保存和載入
- 在 Inspector 中使用
- 作為數據容器

請提供物品數據 Resource 示例。
```

**示例:**
```gdscript
# ItemData.gd
extends Resource
class_name ItemData

@export var item_name: String = "Item"
@export var icon: Texture2D
@export var value: int = 0
@export var description: String = ""
@export_enum("Weapon", "Consumable", "Material") var type: String = "Material"
```

### 5. 場景樹操作

**提示詞:**
```
Godot 場景樹操作最佳實踐:
- 查找節點 (find_child, get_tree())
- 獲取父節點
- 獲取子節點
- 遍歷節點
- 節點群組 (groups)

請提供常用操作示例。
```

### 6. Autoload (單例) 使用

**提示詞:**
```
創建 Godot Autoload (單例) 腳本:
- 全局管理器
- 在任意腳本中訪問
- 數據持久化
- 事件總線

請提供 GameManager 單例示例。
```

**示例:**
```gdscript
# GameManager.gd (設為 Autoload)
extends Node

var score = 0
var player_health = 100

signal score_changed(new_score)

func add_score(points):
    score += points
    score_changed.emit(score)

# 在其他腳本中使用
func _ready():
    GameManager.add_score(10)
    print(GameManager.score)
```

---

## 📚 提示詞技巧庫

### 常用場景

#### 創建主選單
```
創建 Godot 主選單場景:
- 開始遊戲按鈕 (載入關卡)
- 設定按鈕 (顯示設定畫面)
- 退出按鈕
- 背景音樂
- 按鈕音效
- UI 動畫

請提供:
1. 節點結構
2. 主要腳本 Menu.gd
3. 場景切換代碼
```

#### 創建設定系統
```
創建遊戲設定系統:
- 音量調整 (主音量、音樂、音效)
- 全螢幕切換
- 解析度選項
- 設定保存 (ConfigFile)
- 設定載入

請提供完整實現。
```

#### 創建關卡切換
```
創建 Godot 關卡切換系統:
- 使用 SceneTree.change_scene_to_file()
- 載入畫面 (Loading screen)
- 關卡數據管理
- 進度保存
- 過場動畫

請提供 LevelManager 實現。
```

---

## 🐛 常見問題 AI 提示詞

### 物理問題

```
Godot CharacterBody2D 穿牆問題:

代碼:
[貼上移動代碼]

現象:
- 高速移動時穿牆
- 使用 move_and_slide()

請提供:
1. 問題分析
2. 解決方案
3. 最佳實踐
```

### 動畫問題

```
Godot AnimationPlayer 不播放:

場景結構:
[描述節點]

代碼:
[貼上代碼]

請檢查:
1. 動畫是否正確設置
2. 節點路徑是否正確
3. 播放方法使用
4. 常見錯誤
```

### 信號問題

```
Godot 信號沒有觸發:

信號定義:
[代碼]

連接方式:
[代碼]

請幫助排查問題並提供正確實現。
```

---

## 🚀 快速開發工作流

### 新功能開發流程

```
1. 規劃功能
   ↓
2. 使用 AIHelper 生成提示詞
   ↓
3. 發送給 AI (ChatGPT, Claude 等)
   ↓
4. 獲取代碼和場景設置說明
   ↓
5. 在 Godot 中實現
   ↓
6. 測試和調整
   ↓
7. 如有問題，提供錯誤給 AI 調試
   ↓
8. 優化性能
```

### 場景創建流程

```
1. 描述場景需求
   ↓
2. AI 生成節點結構
   ↓
3. 在 Godot 中創建節點
   ↓
4. AI 生成腳本代碼
   ↓
5. 附加腳本並設置
   ↓
6. AI 提供信號連接步驟
   ↓
7. 完成並測試
```

---

## 🎓 學習資源

### AI 工具

1. **ChatGPT** - 代碼生成、調試
2. **Claude** - 長代碼分析、重構
3. **GitHub Copilot** - 實時補全
4. **Cursor** - 專業代碼編輯

### Godot 學習

- [Godot 官方文檔](https://docs.godotengine.org/)
- [GDQuest](https://www.gdquest.com/)
- [HeartBeast](https://www.youtube.com/user/uheartbeast)
- [KidsCanCode](https://kidscancode.org/)

### 提示詞技巧

#### 分步驟請求

不要:
```
創建一個完整的 RPG 系統
```

而是:
```
第一步: 創建角色數據 Resource
第二步: 創建物品欄系統
第三步: 創建裝備系統
第四步: 整合所有系統
```

#### 要求詳細解釋

```
請提供:
1. 完整代碼
2. 每個方法的作用
3. 為什麼這樣設計
4. Godot 特定的優化
5. 使用示例
```

---

## 🎉 總結

### AI 輔助 Godot 開發的優勢

- ⚡ 快速原型開發
- 📚 學習 GDScript 最佳實踐
- 🐛 快速調試問題
- 💡 獲得多種實現方案
- 🎨 場景設置指南

### 注意事項

- ⚠️ 理解 AI 生成的代碼
- ⚠️ 測試所有功能
- ⚠️ 注意 Godot 版本差異 (3.x vs 4.x)
- ⚠️ 優化性能
- ⚠️ 保持代碼整潔

---

**🎮 使用 AI 加速你的 Godot 遊戲開發！**

**最後更新**: 2025-11-18
**適用版本**: Godot 4.2+
**AI 工具**: ChatGPT, Claude, Copilot 等
