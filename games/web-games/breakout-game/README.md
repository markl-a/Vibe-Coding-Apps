# 🎮 打磚塊遊戲 (Breakout Game)
🤖 **AI-Driven | AI-Native** 🚀

經典的打磚塊遊戲，使用原生 HTML5 Canvas 和 JavaScript 開發，具有流暢的遊戲體驗和精美的視覺效果。

## 📋 遊戲特色

- ✅ 經典的打磚塊玩法
- ✅ 滑鼠/觸控控制
- ✅ 多種耐久度的磚塊
- ✅ 漸進式難度提升
- ✅ 關卡系統
- ✅ 生命值系統
- ✅ 分數記錄與最高分
- ✅ 精美的視覺效果和動畫
- ✅ 響應式設計，支援各種螢幕尺寸

## 🎮 遊戲玩法

### 控制方式
- **滑鼠**：移動滑鼠控制擋板左右移動
- **觸控**：在觸控設備上滑動控制擋板

### 遊戲規則
1. 使用擋板反彈球，擊破所有磚塊
2. 每個磚塊有不同的耐久度：
   - 🔴 **紅色磚塊**：1 次擊破（第1-2行）
   - 🟠 **橙色磚塊**：2 次擊破（第3-4行）
   - 🟡 **黃色磚塊**：3 次擊破（第5行）
3. 每擊破一個磚塊得 10 分
4. 球掉落會損失一條生命
5. 清空所有磚塊進入下一關
6. 每過一關球速會增加 10%

## 🚀 快速開始

### 環境需求
- 任何現代網頁瀏覽器（Chrome、Firefox、Safari、Edge）
- Python 3.x（用於本地開發伺服器）或任何 HTTP 伺服器

### 運行方式

#### 方法 1：使用 Python HTTP 伺服器
```bash
# 進入專案目錄
cd breakout-game

# 啟動伺服器
python -m http.server 8000

# 或使用 npm
npm start
```

然後在瀏覽器中打開 `http://localhost:8000`

#### 方法 2：直接打開
直接在瀏覽器中打開 `index.html` 檔案

#### 方法 3：使用 Live Server (VS Code)
1. 安裝 VS Code 的 Live Server 擴展
2. 右鍵點擊 `index.html`
3. 選擇 "Open with Live Server"

## 📁 專案結構

```
breakout-game/
├── index.html          # 主 HTML 檔案
├── src/
│   └── game.js        # 遊戲邏輯
├── package.json       # 專案設定
└── README.md          # 說明文件
```

## 🎨 遊戲機制

### 磚塊系統
- **耐久度系統**：不同顏色的磚塊需要不同次數才能擊破
- **顏色漸變**：磚塊被擊中後顏色會改變，顯示剩餘耐久度
- **數字顯示**：耐久度大於 1 的磚塊會顯示剩餘次數

### 物理系統
- **角度反彈**：球擊中擋板不同位置會以不同角度反彈
- **速度保持**：球的速度在反彈時保持不變
- **邊界檢測**：球會在牆壁上反彈

### 關卡系統
- 每關清空所有磚塊後自動進入下一關
- 球速每關增加 10%
- 關卡轉換時會顯示關卡編號

### 計分系統
- 每擊破一個磚塊得 10 分
- 最高分儲存在瀏覽器本地儲存中
- 新紀錄時會有特殊提示

## 💻 技術實現

### 核心技術
- **HTML5 Canvas**：圖形繪製
- **JavaScript ES6**：遊戲邏輯
- **localStorage**：分數儲存
- **requestAnimationFrame**：遊戲循環

### 主要功能

#### 碰撞檢測
```javascript
// 矩形碰撞檢測（球與磚塊）
if (ball.x + ball.radius > brick.x &&
    ball.x - ball.radius < brick.x + brick.width &&
    ball.y + ball.radius > brick.y &&
    ball.y - ball.radius < brick.y + brick.height) {
    // 碰撞處理
}
```

#### 角度反彈
```javascript
// 根據擊中擋板的位置計算反彈角度
const hitPos = (ball.x - paddle.x) / paddle.width;
const angle = (hitPos - 0.5) * Math.PI / 3; // -60° to 60°
```

#### 漸層效果
```javascript
// 擋板漸層
const gradient = ctx.createLinearGradient(x, y, x, y + height);
gradient.addColorStop(0, '#667eea');
gradient.addColorStop(1, '#764ba2');
ctx.fillStyle = gradient;
```

## 🎯 遊戲參數調整

你可以在 `game.js` 中調整以下參數來改變遊戲難度：

```javascript
// 擋板大小
const PADDLE_WIDTH = 100;      // 增加擋板寬度會降低難度

// 球的速度
ball: {
    dx: 4,                     // 水平速度
    dy: -4,                    // 垂直速度
}

// 磚塊配置
const BRICK_ROWS = 5;          // 磚塊行數
const BRICK_COLS = 8;          // 磚塊列數

// 生命值
lives: 3,                      // 初始生命數
```

## 🤖 AI 輔助開發建議

### 功能擴展提示詞

#### 1. 添加道具系統
```
"在我的打磚塊遊戲中添加道具系統：
- 擊破磚塊隨機掉落道具
- 道具類型：加長擋板、減速球、額外生命
- 道具持續時間為 10 秒
- 請修改現有代碼並添加道具邏輯"
```

#### 2. 添加粒子效果
```
"為打磚塊遊戲添加粒子效果：
- 磚塊被擊破時產生粒子爆炸效果
- 粒子顏色與磚塊顏色相同
- 粒子向四周散開並逐漸消失
- 使用 Canvas 繪製粒子"
```

#### 3. 添加音效
```
"為遊戲添加音效系統：
- 球擊中擋板的音效
- 磚塊被擊破的音效
- 球掉落的音效
- 過關的音效
- 使用 Web Audio API 實現"
```

## 🔧 常見問題

### Q: 遊戲在某些瀏覽器中無法運行？
**A:** 確保使用現代瀏覽器（Chrome 60+, Firefox 60+, Safari 12+, Edge 79+）

### Q: 觸控控制不靈敏？
**A:** 確認在觸控設備上測試，並檢查 `preventDefault()` 是否正常工作

### Q: 如何重置最高分？
**A:** 在瀏覽器控制台執行：
```javascript
localStorage.removeItem('breakoutHighScore');
location.reload();
```

### Q: 如何調整遊戲難度？
**A:** 修改 `game.js` 中的常量，例如增加 `PADDLE_WIDTH` 降低難度

## 📈 未來擴展方向

### 初級擴展
- [ ] 添加背景音樂和音效
- [ ] 添加更多關卡
- [ ] 添加暫停菜單界面
- [ ] 添加設置選項（音量、難度）

### 中級擴展
- [ ] 道具系統（加長擋板、減速球、額外生命、多球）
- [ ] 粒子效果（磚塊爆炸、尾跡效果）
- [ ] 特殊磚塊（金屬磚塊、炸彈磚塊）
- [ ] 關卡編輯器

### 高級擴展
- [ ] 在線排行榜
- [ ] 關卡分享系統
- [ ] 多人對戰模式
- [ ] 成就系統

## 🎓 學習要點

這個專案展示了以下遊戲開發概念：

1. **遊戲循環**：使用 `requestAnimationFrame` 實現流暢的遊戲循環
2. **碰撞檢測**：矩形與圓形的碰撞檢測算法
3. **物理模擬**：簡單的物理引擎（反彈、角度計算）
4. **狀態管理**：遊戲狀態的組織和管理
5. **Canvas 繪圖**：圖形繪製、漸層效果
6. **事件處理**：滑鼠和觸控事件
7. **本地儲存**：使用 localStorage 儲存資料

## 📝 程式碼風格

- 使用 ES6+ 語法
- 清晰的變數命名
- 適當的註解
- 模組化的函數設計

## 🤝 貢獻

歡迎提交 Issue 和 Pull Request！

## 📄 授權

MIT License

---

**🎮 享受遊戲開發的樂趣！使用 AI 工具可以快速添加新功能和改進遊戲！**

**最後更新**: 2025-11-17
**版本**: 1.0.0
**維護狀態**: ✅ 活躍開發
