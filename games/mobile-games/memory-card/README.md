# 🧠 記憶翻牌遊戲 (Memory Card Game)
🤖 **AI-Native | React Native** 🚀

經典的記憶配對遊戲，訓練記憶力和專注力，加入 AI 提示功能輔助學習。

## 📋 遊戲特色

- ✅ 經典記憶翻牌玩法
- ✅ **AI 提示系統**（高亮可配對的卡片）
- ✅ 三種難度級別
  - 簡單：3x4 網格（12 張卡片，6 對）
  - 中等：4x4 網格（16 張卡片，8 對）
  - 困難：4x5 網格（20 張卡片，10 對）
- ✅ 步數追蹤系統
- ✅ 最佳成績記錄
- ✅ 流暢的翻牌動畫
- ✅ 精美的卡片設計
- ✅ 支援 iOS 和 Android

## 🎮 遊戲玩法

### 遊戲規則
1. 點擊卡片翻開查看圖案
2. 每次可以翻開兩張卡片
3. 如果兩張卡片圖案相同，則配對成功並保持翻開
4. 如果不同，卡片會再次翻回
5. 記住卡片位置，找到所有配對即獲勝
6. 挑戰使用最少步數完成遊戲

### AI 提示功能
- 點擊 "💡 AI 提示" 按鈕
- AI 會短暫高亮一對可以配對的卡片
- 幫助初學者或遺忘位置時使用
- 提示會在 2 秒後自動消失

## 🚀 快速開始

```bash
cd memory-card
npm install
npm start
```

## 🎯 核心功能

### 1. 卡片洗牌算法

```javascript
const shuffled = cardPairs
  .map((symbol, index) => ({
    symbol,
    id: index,
    originalIndex: Math.floor(index / 2)
  }))
  .sort(() => Math.random() - 0.5);
```

### 2. 配對檢測

```javascript
const checkMatch = (indices) => {
  const [first, second] = indices;
  const firstCard = cards[first];
  const secondCard = cards[second];

  if (firstCard.symbol === secondCard.symbol) {
    // 配對成功
    setMatchedPairs([...matchedPairs, firstCard.originalIndex]);
  }
};
```

### 3. AI 提示演算法

```javascript
const getAIHint = () => {
  // 找到未配對的卡片
  const unmatched = cards.filter(
    card => !matchedPairs.includes(card.originalIndex)
  );

  // 找到第一對可以配對的卡片並高亮
  for (let i = 0; i < unmatched.length; i++) {
    for (let j = i + 1; j < unmatched.length; j++) {
      if (unmatched[i].symbol === unmatched[j].symbol) {
        highlightPair([i, j]);
        return;
      }
    }
  }
};
```

## 💡 學習價值

這個遊戲可以訓練：
1. **短期記憶**: 記住翻開的卡片位置
2. **視覺記憶**: 識別和記住圖案
3. **專注力**: 保持注意力集中
4. **策略思考**: 優化翻牌順序以減少步數

## 🔧 自訂設定

### 更換卡片圖案

```javascript
const CARD_SYMBOLS = [
  '🎮', '🎯', '🎨', '🎭', '🎪', '🎡', '🎢', '🎰'
];
// 可以改為動物、水果、數字等
```

### 調整難度

```javascript
case 'custom':
  return { pairs: 12, grid: [4, 6] }; // 自訂 4x6 網格
```

## 📊 性能優化

- 使用 `originalIndex` 追蹤配對而不是符號比較
- 及時清理已配對的卡片狀態
- 優化渲染邏輯避免不必要的重繪

## 🎓 擴展建議

- [ ] 添加翻牌音效
- [ ] 添加計時模式
- [ ] 多人對戰模式
- [ ] 自訂卡片主題
- [ ] 排行榜系統
- [ ] 成就系統

## 📄 授權

MIT License

---

**訓練你的大腦，挑戰最少步數！🧠**
