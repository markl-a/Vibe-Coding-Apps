# 井字遊戲 (Tic-Tac-Toe)
🤖 **AI-Driven | AI-Native** 🚀

使用 React Native 和 Expo 開發的經典井字遊戲（圈圈叉叉），支援 iOS 和 Android 平台。

## 📋 遊戲特色

- ✅ 經典的井字遊戲玩法
- ✅ 雙人對戰模式
- ✅ 計分系統（追踪 X、O 和平局）
- ✅ 流暢的動畫效果
- ✅ 獲勝提示和高亮顯示
- ✅ 精美的 UI 設計
- ✅ 支援 iOS 和 Android
- ✅ 響應式佈局

## 🎮 遊戲玩法

### 遊戲規則
1. 兩位玩家輪流在 3x3 的格子中放置 X 和 O
2. 先將三個相同符號連成一線者獲勝
3. 連線可以是橫向、縱向或斜向
4. 如果格子填滿仍無人獲勝則為平局

### 控制方式
- 點擊空白格子放置符號
- 點擊「重新開始」按鈕開始新一局
- 點擊「清除分數」按鈕重置所有分數

## 🚀 快速開始

### 環境需求

- **Node.js** 14.x 或更新版本
- **npm** 或 **yarn**
- **Expo CLI**
- **iOS**: Xcode（macOS）或 Expo Go app
- **Android**: Android Studio 或 Expo Go app

### 安裝步驟

#### 1. 安裝依賴

```bash
# 進入專案目錄
cd tic-tac-toe

# 安裝依賴
npm install

# 或使用 yarn
yarn install
```

#### 2. 啟動開發伺服器

```bash
# 啟動 Expo 開發伺服器
npm start

# 或使用 yarn
yarn start
```

#### 3. 運行應用

**在實體設備上運行**：
1. 在 iOS 或 Android 設備上安裝 Expo Go app
2. 掃描終端或瀏覽器中的 QR code

**在模擬器上運行**：
```bash
# iOS 模擬器（需要 macOS 和 Xcode）
npm run ios

# Android 模擬器
npm run android
```

**在網頁上運行**：
```bash
npm run web
```

## 📁 專案結構

```
tic-tac-toe/
├── App.js              # 主應用組件
├── package.json        # 專案設定
├── app.json           # Expo 配置
├── babel.config.js    # Babel 配置
└── README.md          # 說明文件
```

## 💻 技術實現

### 核心技術
- **React Native** - 跨平台移動應用框架
- **Expo** - React Native 開發工具鏈
- **React Hooks** - useState, useEffect
- **Animated API** - 流暢的動畫效果
- **StyleSheet** - 樣式管理

### 主要功能

#### 狀態管理
```javascript
const [board, setBoard] = useState(Array(9).fill(null));
const [isXNext, setIsXNext] = useState(true);
const [scores, setScores] = useState({ X: 0, O: 0, draws: 0 });
```

#### 勝利判斷
```javascript
const calculateWinner = (squares) => {
  const lines = [
    [0, 1, 2], [3, 4, 5], [6, 7, 8], // 橫向
    [0, 3, 6], [1, 4, 7], [2, 5, 8], // 縱向
    [0, 4, 8], [2, 4, 6],            // 斜向
  ];
  // 檢查每條線是否有獲勝者
};
```

#### 動畫效果
```javascript
Animated.sequence([
  Animated.timing(animations[index], {
    toValue: 0.8,
    duration: 100,
    useNativeDriver: true,
  }),
  Animated.timing(animations[index], {
    toValue: 1,
    duration: 100,
    useNativeDriver: true,
  }),
]).start();
```

## 🎨 UI 設計

### 配色方案
- **背景色**：深藍色系 (#1a1a2e)
- **X 符號**：青綠色 (#4ecdc4)
- **O 符號**：紅色 (#ff6b6b)
- **獲勝高亮**：粉紅色 (#e94560)

### 佈局設計
- 響應式格子大小（根據螢幕寬度計算）
- 圓角設計提升美觀度
- 陰影效果增加立體感
- 清晰的計分板顯示

## 🤖 AI 輔助開發建議

### 功能擴展提示詞

#### 1. 添加 AI 對手
```
"為井字遊戲添加 AI 對手功能：
- 使用 Minimax 演算法實現不敗 AI
- 添加難度選擇（簡單、中等、困難）
- 簡單模式隨機下棋
- 困難模式使用最優策略
- 添加單人模式切換按鈕"
```

#### 2. 添加音效
```
"為遊戲添加音效：
- 下棋時的點擊音效
- 獲勝時的慶祝音效
- 平局時的音效
- 使用 expo-av 套件
- 添加靜音按鈕"
```

#### 3. 添加線上對戰
```
"實現線上對戰功能：
- 使用 Firebase Realtime Database
- 實現房間系統
- 添加等待對手界面
- 實現實時同步
- 添加聊天功能"
```

#### 4. 添加遊戲歷史
```
"添加遊戲歷史記錄：
- 使用 AsyncStorage 儲存歷史
- 顯示最近 10 局的結果
- 添加歷史查看頁面
- 顯示遊戲時間和日期
- 支援清除歷史"
```

## 🎯 擴展功能建議

### 初級擴展
- [ ] 添加音效和音樂
- [ ] 添加深色/淺色主題切換
- [ ] 添加玩家名稱設定
- [ ] 添加遊戲統計（勝率等）

### 中級擴展
- [ ] AI 對手（Minimax 演算法）
- [ ] 不同難度級別
- [ ] 遊戲歷史記錄
- [ ] 自訂遊戲主題

### 高級擴展
- [ ] 線上多人對戰
- [ ] 排行榜系統
- [ ] 成就系統
- [ ] 4x4 或 5x5 的變體模式

## 🔧 常見問題

### Q: 如何在實體設備上測試？
**A:**
1. 安裝 Expo Go app（iOS App Store 或 Google Play）
2. 運行 `npm start`
3. 使用 Expo Go 掃描 QR code

### Q: 如何更改格子大小？
**A:** 修改 `App.js` 中的 `CELL_SIZE` 常量：
```javascript
const CELL_SIZE = (width - 80) / 3; // 調整這裡的數值
```

### Q: 如何更改配色？
**A:** 修改 `styles` 對象中的顏色值：
```javascript
xText: {
  color: '#4ecdc4', // 修改 X 的顏色
},
oText: {
  color: '#ff6b6b', // 修改 O 的顏色
},
```

### Q: 如何添加 AI 對手？
**A:** 參考上面的 AI 輔助開發建議，使用 Minimax 演算法實現

## 📚 學習要點

這個專案展示了以下 React Native 開發概念：

1. **React Hooks**：useState 和 useEffect 的使用
2. **組件設計**：功能性組件的設計模式
3. **狀態管理**：遊戲狀態的管理和更新
4. **動畫**：Animated API 的使用
5. **樣式設計**：StyleSheet 的使用
6. **事件處理**：觸控事件的處理
7. **條件渲染**：根據狀態渲染不同的 UI

## 🎓 演算法說明

### 勝利判斷演算法

遊戲使用預定義的獲勝線陣列來檢查是否有玩家獲勝：

```javascript
const lines = [
  [0, 1, 2], [3, 4, 5], [6, 7, 8], // 橫向三條線
  [0, 3, 6], [1, 4, 7], [2, 5, 8], // 縱向三條線
  [0, 4, 8], [2, 4, 6],            // 對角線兩條
];
```

時間複雜度：O(1) - 固定的 8 條線需要檢查
空間複雜度：O(1) - 固定的資料結構

### 可能的 AI 實現（Minimax）

```javascript
function minimax(board, depth, isMaximizing) {
  const winner = calculateWinner(board);

  if (winner) return winner === 'X' ? 10 - depth : depth - 10;
  if (board.every(cell => cell !== null)) return 0;

  if (isMaximizing) {
    let bestScore = -Infinity;
    for (let i = 0; i < 9; i++) {
      if (board[i] === null) {
        board[i] = 'X';
        const score = minimax(board, depth + 1, false);
        board[i] = null;
        bestScore = Math.max(score, bestScore);
      }
    }
    return bestScore;
  } else {
    let bestScore = Infinity;
    for (let i = 0; i < 9; i++) {
      if (board[i] === null) {
        board[i] = 'O';
        const score = minimax(board, depth + 1, true);
        board[i] = null;
        bestScore = Math.min(score, bestScore);
      }
    }
    return bestScore;
  }
}
```

## 📊 性能優化

### 優化建議
1. 使用 `useCallback` 包裹事件處理函數
2. 使用 `useMemo` 快取計算結果
3. 使用 `React.memo` 避免不必要的重新渲染
4. 使用 `useNativeDriver` 提升動畫性能

## 🤝 貢獻

歡迎提交 Issue 和 Pull Request！

## 📄 授權

MIT License

---

**🎮 享受遊戲開發的樂趣！使用 AI 工具可以快速添加新功能！**

**最後更新**: 2025-11-17
**版本**: 1.0.0
**平台**: iOS, Android, Web
**維護狀態**: ✅ 活躍開發
