# Nuxt Store - Nuxt 3 線上商店

使用 Nuxt 3、Vue 3、TypeScript、Tailwind CSS 和 Pinia 打造的現代化線上商店。

## 功能特色

- 🛍️ **商品展示** - 精美的商品列表與分類篩選
- 🛒 **購物車系統** - 完整的購物車功能與 Pinia 狀態管理
- 💳 **結帳流程** - 直觀的結帳頁面與表單驗證
- 📱 **響應式設計** - 完美支援各種螢幕尺寸
- ⚡ **高效能** - Nuxt 3 SSR 與自動優化
- 🎨 **精美 UI** - 使用 Tailwind CSS 與 Nuxt UI
- 🔄 **狀態管理** - 使用 Pinia 管理全局狀態
- 💾 **持久化** - 購物車資料自動儲存

## 頁面結構

- **首頁** (`/`) - 品牌介紹、功能特色與分類導覽
- **商品列表** (`/products`) - 商品展示與分類篩選
- **購物車** (`/cart`) - 購物車管理與訂單摘要
- **結帳** (`/checkout`) - 訂單結帳與配送資訊填寫

## 技術棧

- **框架**: Nuxt 3
- **UI 框架**: Vue 3 (Composition API)
- **語言**: TypeScript
- **樣式**: Tailwind CSS + Nuxt UI
- **狀態管理**: Pinia
- **部署**: Vercel / Netlify (推薦)

## 快速開始

### 安裝依賴

```bash
npm install
```

### 開發模式

```bash
npm run dev
```

開啟瀏覽器訪問 [http://localhost:3000](http://localhost:3000)

### 建置生產版本

```bash
npm run build
npm run preview
```

### 生成靜態網站

```bash
npm run generate
```

## 專案結構

```
nuxt-store/
├── app.vue                # 根組件
├── nuxt.config.ts         # Nuxt 配置
├── pages/                 # 頁面目錄 (自動路由)
│   ├── index.vue         # 首頁
│   ├── products.vue      # 商品列表
│   ├── cart.vue          # 購物車
│   └── checkout.vue      # 結帳頁面
├── components/           # 組件目錄 (自動導入)
│   ├── AppHeader.vue    # 導航列
│   └── AppFooter.vue    # 頁尾
├── layouts/              # 佈局目錄
│   └── default.vue      # 預設佈局
├── stores/               # Pinia Store
│   └── cart.ts          # 購物車 Store
├── assets/              # 資源目錄
│   └── css/
│       └── main.css     # 全局樣式
├── public/              # 靜態資源
├── package.json         # 專案配置
├── tailwind.config.js   # Tailwind CSS 配置
└── tsconfig.json        # TypeScript 配置
```

## 核心功能說明

### 購物車系統

使用 Pinia 實作購物車狀態管理，支援：
- 新增商品到購物車
- 調整商品數量
- 移除商品
- 計算總價與總件數
- 自動持久化儲存

### 商品管理

- 商品列表展示
- 分類篩選系統
- 商品詳細資訊
- 響應式圖片

### 結帳流程

- 配送資訊表單
- 付款方式選擇
- 訂單摘要確認
- 表單驗證

## 客製化指南

### 新增商品

編輯 `pages/products.vue` 中的 `products` 陣列：

```typescript
const products = [
  {
    id: 1,
    name: '商品名稱',
    price: 1000,
    image: '圖片網址',
    category: '類別',
    description: '商品描述',
  },
  // 新增更多商品...
]
```

### 修改配色

編輯 `tailwind.config.js` 中的 primary 顏色：

```javascript
colors: {
  primary: {
    50: '#f0f9ff',
    // ... 其他色階
  },
}
```

### 自訂頁面內容

- **首頁**: 編輯 `pages/index.vue`
- **Header**: 編輯 `components/AppHeader.vue`
- **Footer**: 編輯 `components/AppFooter.vue`

### 添加新頁面

在 `pages/` 目錄下創建 `.vue` 檔案，Nuxt 會自動生成路由：

```
pages/about.vue → /about
pages/contact.vue → /contact
```

## 部署

### Vercel (推薦)

[![Deploy with Vercel](https://vercel.com/button)](https://vercel.com/new)

1. 將專案推送到 GitHub
2. 在 Vercel 中匯入專案
3. 自動部署完成

### Netlify

```bash
npm run generate
netlify deploy --prod --dir=.output/public
```

### 靜態託管

```bash
npm run generate
# 將 .output/public 目錄部署到任何靜態託管服務
```

## Nuxt 3 特色

### 自動導入

- 組件自動導入 (無需手動 import)
- Composables 自動導入
- Vue API 自動導入

### 檔案系統路由

- 基於 `pages/` 目錄自動生成路由
- 支援動態路由與巢狀路由

### 伺服器端渲染 (SSR)

- 更好的 SEO
- 更快的首次載入
- 可選靜態生成 (SSG)

## 優化建議

- 🔌 **API 整合** - 串接實際的商品 API
- 💳 **支付整合** - 整合第三方支付服務
- 🔐 **使用者認證** - 使用 Nuxt Auth 模組
- 📊 **訂單管理** - 建立訂單追蹤系統
- 🔍 **搜尋功能** - 新增商品搜尋
- ⭐ **評論系統** - 商品評價與評論
- 📧 **Email 通知** - 使用 Nuxt Mailer
- 🎯 **推薦系統** - 相關商品推薦
- 🌙 **深色模式** - 使用 @nuxtjs/color-mode
- 📱 **PWA** - 使用 @vite-pwa/nuxt

## 環境變數

創建 `.env` 檔案：

```env
# API 端點
NUXT_PUBLIC_API_URL=your_api_url

# 其他配置
NUXT_PUBLIC_STRIPE_KEY=your_stripe_key
```

## 模組推薦

```bash
# SEO 優化
npm install @nuxtjs/seo

# 圖片優化
npm install @nuxt/image

# PWA 支援
npm install @vite-pwa/nuxt

# 深色模式
npm install @nuxtjs/color-mode
```

## License

MIT License

## 相關資源

- [Nuxt 3 文檔](https://nuxt.com/docs)
- [Vue 3 文檔](https://vuejs.org/)
- [Pinia 文檔](https://pinia.vuejs.org/)
- [Tailwind CSS 文檔](https://tailwindcss.com/docs)
- [Nuxt UI](https://ui.nuxt.com/)

---

**建立日期**: 2025-11-16
**狀態**: ✅ 可用
**版本**: 1.0.0
