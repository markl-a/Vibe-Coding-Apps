# 🤖 AI 功能指南

Real-time Messenger 現在配備了智能 AI 助手和多種進階功能!

## ✨ 新增功能

### 1. AI 聊天機器人

在任何聊天室中,只要 @提及 AI助手,就會得到智能回覆!

#### 使用方式

```
@AI助手 你好
@AI 什麼是 React?
@助手 推薦學習路線
@bot 幫助
```

#### AI 能做什麼?

##### 📚 知識問答
AI 內建以下技術知識庫:
- JavaScript / TypeScript
- React / Next.js
- Node.js
- Socket.io
- Prisma
- Tailwind CSS

**範例:**
```
@AI助手 什麼是 TypeScript?
→ ◆ TypeScript 是 JavaScript 的超集,加入了靜態型別檢查...
```

##### 💬 智能對話
- 問候回應
- 情緒識別與安慰
- 閒聊陪伴

**範例:**
```
@AI 我今天很開心!
→ 太好了!我也為你感到開心!😊

@AI 有點沮喪...
→ 別難過,一切都會好起來的!如果需要聊聊,我都在。💙
```

##### 🔧 實用工具
- 簡單計算
- 時間查詢
- 學習建議

**範例:**
```
@AI 125 + 375
→ 計算結果: 125 + 375 = 500 🧮

@AI 現在幾點?
→ ⏰ 現在時間:
   2025年11月18日 星期一
   下午 2:30:15
```

##### 📖 常見問題
- 如何使用
- 功能介紹
- 技術棧說明

**範例:**
```
@AI 這個聊天室有什麼功能?
→ 💡 這個聊天室支援:
   • 即時訊息
   • 多個聊天室
   • 在線用戶列表
   • 打字指示器
   • 表情符號
   • AI 助手
```

### 2. 訊息反應 (Emoji Reactions)

為訊息添加表情符號反應,表達你的情緒!

**Socket 事件:**
```javascript
socket.emit('message:react', {
  roomId: 'room-id',
  messageId: 'msg-id',
  emoji: '👍',
  userId: 'user-id',
  username: 'username'
})
```

**功能:**
- 點擊即可添加反應
- 再次點擊取消反應
- 顯示誰對訊息做出了反應
- 支援多種表情符號

### 3. 私人訊息

向特定用戶發送私密訊息,不會在聊天室中顯示。

**Socket 事件:**
```javascript
socket.emit('message:private', {
  toUserId: 'recipient-user-id',
  fromUserId: 'your-user-id',
  fromUsername: 'your-username',
  content: '私密訊息內容'
})
```

**接收私人訊息:**
```javascript
socket.on('message:private', (message) => {
  console.log('收到私訊:', message)
})
```

### 4. 訊息編輯

編輯已發送的訊息(僅限自己的訊息)。

**Socket 事件:**
```javascript
socket.emit('message:edit', {
  roomId: 'room-id',
  messageId: 'msg-id',
  userId: 'your-user-id',
  newContent: '修改後的內容'
})
```

**效果:**
- 訊息內容更新
- 顯示「已編輯」標記
- 記錄編輯時間

### 5. 訊息刪除

刪除自己發送的訊息。

**Socket 事件:**
```javascript
socket.emit('message:delete', {
  roomId: 'room-id',
  messageId: 'msg-id',
  userId: 'your-user-id'
})
```

## 🎯 完整事件列表

### 服務器端事件

| 事件名稱 | 說明 | 參數 |
|---------|------|------|
| `room:list` | 請求聊天室列表 | 無 |
| `room:create` | 建立新聊天室 | `{name, description}` |
| `room:join` | 加入聊天室 | `{roomId}` |
| `room:leave` | 離開聊天室 | `{roomId}` |
| `message:send` | 發送訊息 | `{roomId, userId, username, content, timestamp}` |
| `message:react` | 對訊息添加反應 | `{roomId, messageId, emoji, userId, username}` |
| `message:edit` | 編輯訊息 | `{roomId, messageId, userId, newContent}` |
| `message:delete` | 刪除訊息 | `{roomId, messageId, userId}` |
| `message:private` | 發送私密訊息 | `{toUserId, fromUserId, fromUsername, content}` |
| `typing:start` | 開始輸入 | `{roomId, userId, username}` |
| `typing:stop` | 停止輸入 | `{roomId, userId}` |

### 客戶端監聽事件

| 事件名稱 | 說明 | 接收數據 |
|---------|------|---------|
| `room:list` | 接收聊天室列表 | `Room[]` |
| `message:history` | 接收歷史訊息 | `Message[]` |
| `message:new` | 接收新訊息 | `Message` |
| `message:reaction` | 訊息反應更新 | `{messageId, reactions}` |
| `message:edited` | 訊息已編輯 | `{messageId, content, editedAt}` |
| `message:deleted` | 訊息已刪除 | `{messageId}` |
| `message:private` | 接收私密訊息 | `PrivateMessage` |
| `message:private:sent` | 私訊發送確認 | `PrivateMessage` |
| `message:private:error` | 私訊發送失敗 | `{error, toUserId}` |
| `users:online` | 在線用戶更新 | `User[]` |
| `user:joined` | 用戶加入 | `User` |
| `user:left` | 用戶離開 | `User` |
| `typing:start` | 用戶開始輸入 | `{userId, username, roomId}` |
| `typing:stop` | 用戶停止輸入 | `{userId, roomId}` |

## 📊 AI 機器人特性

### 智能程度
- ✅ 關鍵字識別
- ✅ 情緒分析
- ✅ 上下文理解
- ✅ 隨機回覆變化
- ✅ 思考延遲模擬 (1-2秒)

### 知識範圍
- 🔧 前端開發 (React, Next.js, TypeScript)
- 🔧 後端開發 (Node.js, Express)
- 🔧 資料庫 (Prisma, PostgreSQL)
- 🔧 即時通訊 (Socket.io)
- 🔧 樣式設計 (Tailwind CSS)

### 個性特點
- 😊 友善熱情
- 🤝 樂於助人
- 📚 知識豐富
- 💬 善於聊天
- 🎯 回覆精準

## 🔮 未來功能(規劃中)

### AI 增強
- [ ] 整合真實 AI API (OpenAI, Claude)
- [ ] 多語言翻譯
- [ ] 圖片識別與描述
- [ ] 語音轉文字
- [ ] 程式碼解釋與除錯建議

### 訊息功能
- [ ] 訊息搜尋
- [ ] 訊息標籤
- [ ] 訊息釘選
- [ ] 訊息轉發
- [ ] 引用回覆

### 聊天室功能
- [ ] 聊天室密碼保護
- [ ] 聊天室管理員
- [ ] 用戶權限管理
- [ ] 聊天室主題設定

### 媒體功能
- [ ] 檔案分享
- [ ] 圖片上傳
- [ ] 語音訊息
- [ ] 視訊通話

## 💡 使用技巧

### 1. 高效使用 AI 助手
- 使用明確的問題
- 一次問一個主題
- 善用關鍵字

**好的問題範例:**
```
✅ @AI 什麼是 React?
✅ @AI 推薦學習 JavaScript 的方法
✅ @AI 125 + 375 = ?
```

**不太好的問題:**
```
❌ @AI 那個
❌ @AI 幫我
❌ @AI 你知道嗎
```

### 2. 訊息管理
- 長按訊息查看操作選項
- 快速添加表情反應
- 及時編輯錯誤訊息

### 3. 私人訊息
- 點擊用戶頭像發起私聊
- 私訊通知獨立顯示
- 僅在雙方在線時可用

## 🛠️ 開發者信息

### 自訂 AI 回覆

編輯 `server/aiBot.js`:

```javascript
// 添加新知識
this.knowledge = {
  'your-keyword': '你的回覆內容',
  // ...
}

// 添加新 FAQ
this.faq = {
  '你的問題': '你的答案',
  // ...
}
```

### 擴展 AI 功能

```javascript
// 在 aiBot.js 中添加新方法
answerCustomQuestion(question) {
  if (question.includes('關鍵字')) {
    return '自訂回覆'
  }
  return null
}

// 在 generateResponse 中調用
const customResponse = this.answerCustomQuestion(q)
if (customResponse) {
  return customResponse
}
```

## 📞 獲取協助

遇到問題? 試試這些:
1. 在聊天室輸入 `@AI助手 幫助`
2. 查看伺服器控制台日誌
3. 檢查網路連接
4. 重新載入頁面

---

**享受升級後的聊天體驗!** 🎉
