---
title: "Building a Modern Blog Platform with Next.js 14 and AI"
date: "2025-11-15"
description: "Learn how to build a feature-rich, AI-powered blog platform using Next.js 14, TypeScript, and modern web technologies."
tags: ["Next.js", "TypeScript", "AI", "Tutorial", "Web Development"]
author: "DevBlog Team"
image: "/images/nextjs-blog.jpg"
---

# Building a Modern Blog Platform with Next.js 14 and AI

Creating a developer blog in 2025 means more than just displaying markdown files. Today's readers expect interactive experiences, intelligent features, and blazing-fast performance. Let's build a modern blog platform that delivers all of this and more.

## Project Overview

We'll create a full-featured blog with:

- **Next.js 14** App Router for optimal performance
- **AI-powered features** for enhanced user experience
- **MDX support** for interactive content
- **TypeScript** for type safety
- **Tailwind CSS** for beautiful styling

## Setting Up the Foundation

### 1. Initialize the Project

```bash
npx create-next-app@latest my-blog --typescript --tailwind --app
cd my-blog
```

### 2. Install Essential Dependencies

```bash
npm install next-mdx-remote gray-matter reading-time date-fns
npm install lucide-react clsx tailwind-merge
npm install @tailwindcss/typography
```

### 3. Project Structure

```
my-blog/
├── app/
│   ├── layout.tsx
│   ├── page.tsx
│   ├── blog/
│   │   ├── page.tsx
│   │   └── [slug]/page.tsx
│   └── ai-tools/
│       └── page.tsx
├── components/
│   ├── ai/
│   ├── blog/
│   └── ui/
├── content/
│   └── posts/
└── lib/
    ├── mdx.ts
    └── utils.ts
```

## Core Features Implementation

### 1. MDX Blog Posts

Create a utility to read and process MDX files:

```typescript
// lib/mdx.ts
import fs from 'fs';
import path from 'path';
import matter from 'gray-matter';
import readingTime from 'reading-time';

const postsDirectory = path.join(process.cwd(), 'content/posts');

export interface Post {
  slug: string;
  title: string;
  date: string;
  description: string;
  tags: string[];
  content: string;
  readingTime: string;
}

export function getAllPosts(): Post[] {
  const fileNames = fs.readdirSync(postsDirectory);

  const posts = fileNames
    .filter((fileName) => fileName.endsWith('.mdx'))
    .map((fileName) => {
      const slug = fileName.replace(/\.mdx$/, '');
      const fullPath = path.join(postsDirectory, fileName);
      const fileContents = fs.readFileSync(fullPath, 'utf8');
      const { data, content } = matter(fileContents);

      return {
        slug,
        title: data.title,
        date: data.date,
        description: data.description,
        tags: data.tags || [],
        content,
        readingTime: readingTime(content).text,
      };
    })
    .sort((a, b) => (a.date > b.date ? -1 : 1));

  return posts;
}
```

### 2. Dynamic Blog Routes

```typescript
// app/blog/[slug]/page.tsx
import { getAllPosts, getPostBySlug } from '@/lib/mdx';
import { MDXRemote } from 'next-mdx-remote/rsc';
import { notFound } from 'next/navigation';

export async function generateStaticParams() {
  const posts = getAllPosts();
  return posts.map((post) => ({
    slug: post.slug,
  }));
}

export default async function BlogPost({
  params
}: {
  params: { slug: string }
}) {
  const post = getPostBySlug(params.slug);

  if (!post) {
    notFound();
  }

  return (
    <article className="max-w-4xl mx-auto px-4 py-16">
      <header className="mb-8">
        <h1 className="text-5xl font-bold mb-4">
          {post.title}
        </h1>
        <div className="flex gap-4 text-gray-600">
          <time dateTime={post.date}>
            {new Date(post.date).toLocaleDateString()}
          </time>
          <span>{post.readingTime}</span>
        </div>
      </header>

      <div className="prose prose-lg max-w-none">
        <MDXRemote source={post.content} />
      </div>
    </article>
  );
}
```

### 3. Blog Listing Page

```typescript
// app/blog/page.tsx
import { getAllPosts } from '@/lib/mdx';
import BlogCard from '@/components/blog/BlogCard';
import SearchBar from '@/components/blog/SearchBar';

export default function BlogPage() {
  const posts = getAllPosts();

  return (
    <div className="container mx-auto px-4 py-16">
      <h1 className="text-4xl font-bold mb-8">Blog</h1>

      <SearchBar />

      <div className="grid md:grid-cols-2 lg:grid-cols-3 gap-6 mt-8">
        {posts.map((post) => (
          <BlogCard key={post.slug} post={post} />
        ))}
      </div>
    </div>
  );
}
```

## Adding AI Features

### 1. AI Assistant Component

```typescript
// components/ai/AIAssistant.tsx
'use client';

import { useState } from 'react';
import { Sparkles } from 'lucide-react';

export default function AIAssistant({ posts }) {
  const [isOpen, setIsOpen] = useState(false);
  const [query, setQuery] = useState('');

  const handleQuery = async () => {
    // Search through posts
    const results = posts.filter(post =>
      post.title.toLowerCase().includes(query.toLowerCase())
    );

    // Display results...
  };

  return (
    <div className="fixed bottom-6 right-6 z-50">
      <button
        onClick={() => setIsOpen(!isOpen)}
        className="w-14 h-14 bg-primary-600 text-white rounded-full shadow-lg"
      >
        <Sparkles className="w-6 h-6 mx-auto" />
      </button>

      {isOpen && (
        <div className="absolute bottom-16 right-0 w-96 bg-white rounded-lg shadow-2xl">
          {/* Chat interface */}
        </div>
      )}
    </div>
  );
}
```

### 2. Content Generation API

```typescript
// app/api/ai/generate/route.ts
import { NextResponse } from 'next/server';

export async function POST(request: Request) {
  const { prompt, type } = await request.json();

  // In production: Call OpenAI/Anthropic API
  // For now, return simulated response

  const content = generateContent(prompt, type);

  return NextResponse.json({ content });
}
```

## Advanced Features

### 1. Reading Progress Indicator

```typescript
// components/blog/ReadingProgress.tsx
'use client';

import { useEffect, useState } from 'react';

export default function ReadingProgress() {
  const [progress, setProgress] = useState(0);

  useEffect(() => {
    const updateProgress = () => {
      const scrolled = window.scrollY;
      const total = document.documentElement.scrollHeight - window.innerHeight;
      const progress = (scrolled / total) * 100;
      setProgress(progress);
    };

    window.addEventListener('scroll', updateProgress);
    return () => window.removeEventListener('scroll', updateProgress);
  }, []);

  return (
    <div
      className="fixed top-0 left-0 h-1 bg-primary-600 z-50 transition-all"
      style={{ width: `${progress}%` }}
    />
  );
}
```

### 2. Code Playground

```typescript
// components/interactive/CodePlayground.tsx
'use client';

import { useState } from 'react';

export default function CodePlayground() {
  const [code, setCode] = useState('console.log("Hello!");');
  const [output, setOutput] = useState('');

  const runCode = () => {
    try {
      const logs = [];
      const customConsole = {
        log: (...args) => logs.push(args.join(' ')),
      };

      new Function('console', code)(customConsole);
      setOutput(logs.join('\n'));
    } catch (error) {
      setOutput(`Error: ${error.message}`);
    }
  };

  return (
    <div className="grid grid-cols-2 gap-4">
      <div>
        <h3>Code</h3>
        <textarea
          value={code}
          onChange={(e) => setCode(e.target.value)}
          className="w-full h-64 p-4 font-mono bg-gray-900 text-green-400"
        />
        <button onClick={runCode}>Run</button>
      </div>
      <div>
        <h3>Output</h3>
        <pre className="w-full h-64 p-4 bg-gray-900 text-green-400">
          {output}
        </pre>
      </div>
    </div>
  );
}
```

### 3. Smart Recommendations

```typescript
// components/blog/RelatedPosts.tsx
export default function RelatedPosts({ currentPost, allPosts }) {
  const related = allPosts
    .filter(post => post.slug !== currentPost.slug)
    .map(post => {
      let score = 0;

      // Score based on shared tags
      const sharedTags = post.tags.filter(tag =>
        currentPost.tags.includes(tag)
      );
      score += sharedTags.length * 10;

      // Score based on recency
      const daysDiff = Math.abs(
        new Date(post.date) - new Date(currentPost.date)
      ) / (1000 * 60 * 60 * 24);
      score -= daysDiff / 100;

      return { post, score };
    })
    .sort((a, b) => b.score - a.score)
    .slice(0, 3)
    .map(({ post }) => post);

  return (
    <div>
      <h2>Recommended for You</h2>
      {related.map(post => (
        <BlogCard key={post.slug} post={post} />
      ))}
    </div>
  );
}
```

## Performance Optimization

### 1. Image Optimization

```typescript
import Image from 'next/image';

<Image
  src="/images/cover.jpg"
  alt="Cover"
  width={1200}
  height={630}
  priority
  quality={85}
/>
```

### 2. Static Generation

```typescript
export const revalidate = 3600; // Revalidate every hour

export async function generateStaticParams() {
  return getAllPosts().map(post => ({ slug: post.slug }));
}
```

### 3. Bundle Size Optimization

```javascript
// next.config.js
module.exports = {
  experimental: {
    optimizePackageImports: ['lucide-react'],
  },
  images: {
    formats: ['image/avif', 'image/webp'],
  },
};
```

## SEO Best Practices

### 1. Metadata Generation

```typescript
export async function generateMetadata({ params }) {
  const post = getPostBySlug(params.slug);

  return {
    title: post.title,
    description: post.description,
    openGraph: {
      title: post.title,
      description: post.description,
      type: 'article',
      publishedTime: post.date,
    },
  };
}
```

### 2. Structured Data

```typescript
const structuredData = {
  '@context': 'https://schema.org',
  '@type': 'BlogPosting',
  headline: post.title,
  datePublished: post.date,
  author: {
    '@type': 'Person',
    name: post.author,
  },
};
```

### 3. Sitemap Generation

```typescript
// app/sitemap.ts
export default function sitemap() {
  const posts = getAllPosts();

  return posts.map(post => ({
    url: `https://yourdomain.com/blog/${post.slug}`,
    lastModified: post.date,
  }));
}
```

## Deployment

### Vercel (Recommended)

```bash
# Install Vercel CLI
npm i -g vercel

# Deploy
vercel

# Production deployment
vercel --prod
```

### Environment Variables

```bash
# .env.local
OPENAI_API_KEY=sk-...
NEXT_PUBLIC_SITE_URL=https://yourdomain.com
```

## Analytics Integration

```typescript
// Vercel Analytics
import { Analytics } from '@vercel/analytics/react';

export default function RootLayout({ children }) {
  return (
    <html>
      <body>
        {children}
        <Analytics />
      </body>
    </html>
  );
}
```

## Testing

```typescript
// __tests__/blog.test.ts
import { getAllPosts, getPostBySlug } from '@/lib/mdx';

describe('Blog functions', () => {
  test('getAllPosts returns array', () => {
    const posts = getAllPosts();
    expect(Array.isArray(posts)).toBe(true);
  });

  test('getPostBySlug returns post', () => {
    const post = getPostBySlug('test-post');
    expect(post).toBeDefined();
    expect(post.title).toBeTruthy();
  });
});
```

## Best Practices

### 1. File Organization

- Keep components modular and reusable
- Use consistent naming conventions
- Group related files together
- Separate business logic from UI

### 2. Performance

- Use Server Components by default
- Add 'use client' only when needed
- Optimize images with Next.js Image
- Implement proper caching strategies

### 3. Accessibility

- Add proper ARIA labels
- Ensure keyboard navigation works
- Use semantic HTML
- Test with screen readers

### 4. Security

- Validate all user inputs
- Use environment variables for secrets
- Implement rate limiting on APIs
- Sanitize MDX content

## Conclusion

You now have a solid foundation for a modern, AI-powered blog platform. This setup provides:

✅ Fast, SEO-friendly static generation
✅ Interactive AI features
✅ Beautiful, responsive design
✅ Type-safe development
✅ Optimal performance

## Next Steps

1. Add user authentication
2. Implement comment system
3. Create admin dashboard
4. Add newsletter integration
5. Set up analytics
6. Implement A/B testing

## Resources

- [Next.js Documentation](https://nextjs.org/docs)
- [MDX Documentation](https://mdxjs.com/)
- [Tailwind CSS](https://tailwindcss.com/)
- [OpenAI API](https://platform.openai.com/docs)

---

**Happy coding!** Build something amazing and share your knowledge with the world.
